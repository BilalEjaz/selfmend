---
phase: 07-recipes-docs
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - README.md
  - CHANGELOG.md
  - scripts/check-readme-examples.mjs
  - package.json
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "The README documents wrapPage with a named, working recipe for each of Cucumber, Mocha/Jest, and a plain script, each showing scope() wiring, baseline load/save, and heal output"
    - "The README states the never-false-green guarantee for raw mode AND the honest limits so an adopter is not surprised"
    - "The README recipe code blocks are kept in sync with the compilable examples, enforced by a check"
    - "No em dashes appear anywhere in README, CHANGELOG, or the example files"
  artifacts:
    - path: "README.md"
      provides: "Three runner-agnostic recipes + the raw-mode never-false-green + honest-limits section"
      contains: "wrapPage"
    - path: "scripts/check-readme-examples.mjs"
      provides: "README-sync gate: asserts each recipe's fenced block matches its examples/ source file"
      contains: "examples"
    - path: "CHANGELOG.md"
      provides: "0.2.0 release entry describing the runner-agnostic recipes for the public"
      contains: "0.2.0"
  key_links:
    - from: "README.md recipe fenced blocks"
      to: "examples/cucumber.ts, examples/mocha-jest.ts, examples/plain-script.ts"
      via: "scripts/check-readme-examples.mjs asserts byte-equality between each fenced block and its source file"
      pattern: "check-readme-examples"
    - from: "package.json"
      to: "scripts/check-readme-examples.mjs"
      via: "a check:readme npm script in the verify chain"
      pattern: "check:readme"
---

<objective>
Write the documentation an adopter reads: three named runner-agnostic recipes in the README (Cucumber, Mocha/Jest, plain script), the raw-mode never-false-green guarantee, and the honest limits (ROADMAP Phase 7 criteria 1 and 2). Bind the README recipe code blocks to plan 07-01's compilable example files with a sync check so the documented code can never drift from the machine-checked source of truth (criterion 3's last mile). Update the CHANGELOG to a public 0.2.0 entry and enforce the em-dash ban across the docs.

Purpose: Criteria 1 and 2. The recipes must read like a real maintainer wrote them (plain, direct, honest), and the limits must be stated so no adopter is surprised.
Output: README recipe + trust/limits sections, CHANGELOG 0.2.0 entry, a check:readme sync gate wired into verify.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# This plan DEPENDS ON 07-01: the example files are the source of truth for the
# recipe code blocks. Read the 07-01 SUMMARY for the exact import-resolution note
# and any flagged API wart.
@.planning/phases/07-recipes-docs/07-01-SUMMARY.md

@README.md
@CHANGELOG.md
@.planning/REQUIREMENTS.md
@package.json
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the three runner-agnostic recipes + the raw-mode trust/limits section to the README</name>
  <read_first>
    - README.md (the whole current file: keep the v1 fixture-mode content coherent; the existing "Roadmap" section near the end is the placeholder this work replaces with real recipes, and "Limitations"/"Privacy & trust"/"How healing works" are the v1 trust sections to extend, not duplicate)
    - examples/cucumber.ts, examples/mocha-jest.ts, examples/plain-script.ts (the EXACT code that must appear in the fenced blocks; the README code is a copy of these files, this is the sync contract)
    - src/index.ts (confirm the public symbol names referenced in prose match the real exports)
    - .planning/REQUIREMENTS.md (the Out-of-Scope table: Cypress/Selenium reason, BrowserContext-level Page-only reason; the cross-cutting never-false-green hard rule wording)
    - .planning/phases/05-runner-agnostic-core/05-02-SUMMARY.md (the never-false-green-in-raw-mode control proof to cite accurately: wrong/missing key = missed heal)
    - .planning/phases/07-recipes-docs/07-01-SUMMARY.md (any flagged API wart to mention honestly in limits)
  </read_first>
  <action>
    Add a new top-level README section titled exactly "## Using selfmend without @playwright/test" (placed after "Composing with your own fixtures" and replacing the old forward-looking "## Roadmap" placeholder block, since that future is now shipped). Open it with two or three plain sentences: wrapPage(rawPage, opts) returns a bare wrapped page that is a drop-in for the original, so step definitions and page objects need no changes; you supply identity via a scope() callback read live per locator, load/save the baseline yourself, and collect heals via onHeal then renderHealSummary, with no Playwright reporter. No hype, no marketing tone.

    Then three subsections with these EXACT headings:
      "### Plain script"
      "### Cucumber"
      "### Mocha / Jest"
    Each subsection: one or two sentences of plain context, then a single fenced ts code block whose content is BYTE-IDENTICAL to the corresponding examples/ file (plain-script.ts, cucumber.ts, mocha-jest.ts). The sync check (Task 2) enforces this equality, so copy the file content verbatim including the import lines. After each block, one short sentence naming the three things the recipe shows: the scope() wiring, the baseline load and save, and the heal output via renderHealSummary.

    Then a section titled exactly "### The never-false-green guarantee in raw mode" stating, in plain prose, the named claims (criterion 2):
      - never-false-green holds in raw mode exactly as in fixture mode: a heal is accepted only above the confidence floor AND beyond the second-best margin, both enforced in the pure core, so every adapter inherits it;
      - a wrong or missing scope() key is a MISSED heal (the locator fails normally), never a wrong heal and never a false green (cite that this is control-tested);
      - a throwing or absent scope()/onHeal cannot break or slow the run (it fails safe to the coarse default / a no-op).

    Then a section titled exactly "### Honest limits" stating the named limits (criterion 2), sourced from the REQUIREMENTS Out-of-Scope table:
      - Page-level only this milestone: wrapPage heals one Playwright Page; popups/new tabs each need their own wrapPage; whole-BrowserContext wrapping is a later add;
      - Playwright Pages only: it works only with frameworks that drive a real Playwright Page; Cypress and Selenium use incompatible locator models and are out of scope;
      - parallel runs: each worker keeps its own baseline; merge them with mergeBaselines(...) before saving so workers never fight over one file;
      - carry forward the existing v1 occurrence-index and selectOption/setInputFiles caveats by reference (do not duplicate the full text; link to the existing "Limitations" section).

    Keep the existing v1 sections (Install, import-swap, reporter, Configuration, Privacy & trust, Limitations, License) intact and coherent. NO em dashes anywhere in the new prose or code (CLAUDE.md hard rule). Plain maintainer voice throughout; no AI-marketing adjectives.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('README.md','utf8');const need=['## Using selfmend without @playwright/test','### Plain script','### Cucumber','### Mocha / Jest','### The never-false-green guarantee in raw mode','### Honest limits','wrapPage','renderHealSummary','mergeBaselines'];for(const n of need){if(!s.includes(n)){console.error('MISSING: '+n);process.exit(1)}}if(s.includes(String.fromCharCode(8212))){console.error('EM DASH in README');process.exit(2)}console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains the headings: "## Using selfmend without @playwright/test", "### Plain script", "### Cucumber", "### Mocha / Jest", "### The never-false-green guarantee in raw mode", "### Honest limits".
    - Each of the three recipe subsections contains a a fenced ts fenced block; the prose names scope(), baseline load/save, and heal output.
    - The never-false-green section contains the named claims: confidence floor AND margin in the pure core, wrong/missing key = missed heal never a wrong heal, throwing scope/onHeal fails safe.
    - The Honest limits section names: Page-level only / popups need own wrap, Playwright Pages only (Cypress/Selenium out of scope), parallel via mergeBaselines.
    - README.md contains no em dash (U+2014); the verify command exits 0.
    - The existing v1 sections (Install, import swap, Configuration, Privacy & trust, Limitations, License) are still present.
  </acceptance_criteria>
  <done>The README documents all three named recipes with scope()/baseline/heal-output wiring plus the raw-mode never-false-green guarantee and honest limits, em-dash-free and in plain maintainer voice.</done>
</task>

<task type="auto">
  <name>Task 2: Add the README-sync gate binding fenced blocks to the example files</name>
  <read_first>
    - README.md (the three a fenced ts recipe blocks the gate must compare; note the exact heading text the script keys off)
    - examples/cucumber.ts, examples/mocha-jest.ts, examples/plain-script.ts (the source-of-truth files; the gate asserts each fenced block equals the file content)
    - scripts/check-examples.mjs (the sibling docs gate created in 07-01; copy its Windows-safe node-script conventions and comment style)
    - package.json (the scripts block + verify chain, to wire check:readme in)
  </read_first>
  <action>
    Create scripts/check-readme-examples.mjs: a node script that reads README.md and, for each recipe, extracts the a fenced ts fenced block under its heading ("### Plain script", "### Cucumber", "### Mocha / Jest") and asserts it is BYTE-IDENTICAL (after trimming a single trailing newline) to the corresponding examples/ file content (plain-script.ts, cucumber.ts, mocha-jest.ts). On any mismatch or missing block, print which recipe drifted and process.exit(1). Add a top comment stating this enforces the README-recipe-to-examples sync (criterion 3): the examples/ files are the single source of truth, the README embeds them verbatim, and this gate fails if they diverge so a docs edit cannot silently break the documented code. Use only node builtins (fs), no new dependency.

    Pick the embed mechanism explicitly: the README fenced block content is the verbatim copy of the example file (the convention from Task 1). The gate parses by locating the recipe heading then the next a fenced ts block. Keep the parser simple and deterministic; if a heading or block is absent, fail loudly.

    Edit package.json: add "check:readme": "node scripts/check-readme-examples.mjs" and append "&& npm run check:readme" to the verify chain (after check:examples). No new dependency.
  </action>
  <verify>
    <automated>npm run check:readme</automated>
  </verify>
  <acceptance_criteria>
    - scripts/check-readme-examples.mjs exists and compares each README a fenced ts recipe block to its examples/ source file.
    - package.json contains a "check:readme" script and the "verify" chain includes "check:readme".
    - `npm run check:readme` exits 0 (the README blocks match the example files).
    - Deliberately editing one fenced block to differ makes the gate exit non-zero (sync is actually enforced); revert after confirming.
    - No new entry in dependencies or devDependencies.
  </acceptance_criteria>
  <done>A check:readme gate proves each README recipe block is byte-identical to its compilable examples/ source; it is wired into verify; tampering with a block fails the gate.</done>
</task>

<task type="auto">
  <name>Task 3: Cut the CHANGELOG 0.2.0 entry for the public</name>
  <read_first>
    - CHANGELOG.md (the [Unreleased] section that currently forward-describes wrapPage; this becomes the released 0.2.0 entry; match the Keep a Changelog format and the existing 0.1.x entry style)
    - package.json (the current version 0.1.2; this plan does NOT bump version, it only writes the changelog entry; note this in the SUMMARY so the maintainer bumps at release)
    - README.md (the recipe section names, to keep the changelog wording consistent with the docs)
    - .planning/ROADMAP.md (Phase 7 success criteria, to describe what 0.2.0 delivers accurately)
  </read_first>
  <action>
    Convert the CHANGELOG "## [Unreleased]" block into a real "## [0.2.0] - <date>" entry (use today's date 2026-06-02) with Keep a Changelog subsections. Under ### Added, describe for the public: wrapPage(page, { store, config?, onHeal?, scope? }) to heal on any framework driving a real Playwright Page (Cucumber, Mocha/Jest, plain scripts) with no test rewrites; the scope() identity callback; resetScope for retry-safe occurrence reset; standalone loadBaseline/saveBaseline (refresh-and-add only) and mergeBaselines for parallel runs; the onHeal callback and renderHealSummary for a reporter-free boxed summary; and the new README recipes. State the honest scope: Playwright Pages only (not Cypress/Selenium), Page-level only this milestone, never-false-green unchanged. Keep a leading "## [Unreleased]" placeholder above 0.2.0 if the existing repo convention keeps one, otherwise replace it. Add the 0.2.0 link reference line at the bottom matching the existing [0.1.0] link style. NO em dashes; plain voice consistent with the 0.1.x entries.

    Do NOT bump package.json "version" (release versioning is the maintainer's manual step per RELEASING.md). Note in the SUMMARY that the changelog now describes 0.2.0 but package.json is still 0.1.2, so the maintainer bumps + tags at release.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('CHANGELOG.md','utf8');if(!/##\s*\[0\.2\.0\]/.test(s)){console.error('no 0.2.0 entry');process.exit(1)}if(!s.includes('wrapPage')){console.error('0.2.0 entry does not mention wrapPage');process.exit(2)}if(s.includes(String.fromCharCode(8212))){console.error('EM DASH');process.exit(3)}console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md contains a "## [0.2.0] - 2026-06-02" entry whose ### Added text names wrapPage, scope(), loadBaseline/saveBaseline, mergeBaselines, onHeal, and renderHealSummary.
    - The entry states the honest scope (Playwright Pages only; Page-level; never-false-green unchanged).
    - A [0.2.0] link reference line exists matching the existing link style.
    - package.json "version" is unchanged (still 0.1.2).
    - CHANGELOG.md contains no em dash; the verify command exits 0.
  </acceptance_criteria>
  <done>The CHANGELOG has a public, em-dash-free 0.2.0 entry describing the runner-agnostic API and honest scope; version bump is left to the maintainer at release.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adopter reads README -> wires selfmend into their suite | Inaccurate recipe prose or a missing limit could cause the adopter to trust a heal they should not, or wire scope() wrong |
| README recipe text -> compilable examples/ source | The documented code must equal the machine-checked code or the docs lie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | README recipe drifting from the compilable example | mitigate | check:readme asserts byte-equality between each fenced block and its examples/ source; wired into verify |
| T-07-05 | Repudiation | adopter surprised by an undocumented limit | mitigate | the Honest limits + never-false-green sections state Page-only, Cypress/Selenium out of scope, missed-heal-not-wrong-heal, parallel-needs-mergeBaselines (criterion 2) |
| T-07-06 | Information disclosure | a recipe implying data leaves CI | accept | the recipes are reporter-free and offline by construction; the existing Privacy and trust section already states the offline guarantee and is kept intact |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | this plan installs NOTHING; no package-manager install tasks exist, so the legitimacy gate is N/A by construction |
</threat_model>

<verification>
- README documents three named recipes (Plain script, Cucumber, Mocha / Jest) each with scope()/baseline/heal-output wiring, plus the raw-mode never-false-green guarantee and the honest limits.
- npm run check:readme proves every README recipe block matches its compilable examples/ source.
- CHANGELOG has a public 0.2.0 entry; version bump left to the maintainer.
- README, CHANGELOG, and examples are em-dash-free; runtime code untouched; no new deps.
</verification>

<success_criteria>
- Criterion 1: three named, working recipes documented, each showing scope() + baseline load/save + heal output.
- Criterion 2: never-false-green-in-raw-mode + honest-limits sections with the named claims.
- Criterion 3 (last mile): README recipes are bound to the compilable, CI-checked examples by check:readme, so documented code stays runnable as written.
</success_criteria>

<output>
Create `.planning/phases/07-recipes-docs/07-02-SUMMARY.md` when done. Record: that package.json version is still 0.1.2 (maintainer bumps to 0.2.0 + tags at release), and any limit wording that surfaced a real product gap worth flagging.
</output>
