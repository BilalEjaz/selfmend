---
phase: 06-standalone-persistence-output
plan: 02
type: tdd
wave: 2
depends_on: [06-01]
files_modified:
  - src/reporter/render.ts
  - src/reporter/render.test.ts
  - src/reporter/reporter.ts
  - src/reporter/reporter.test.ts
  - src/integration/onheal-confirm.test.ts
  - src/index.ts
autonomous: true
requirements: [OUT-01, OUT-02]
must_haves:
  truths:
    - "A developer renders the standard boxed heal summary from a flat event array via renderHealSummary(events), byte-identical to the reporter's output for the same events"
    - "renderHealSummary is the SINGLE shared pure function; the reporter calls it rather than holding its own copy of the box rendering"
    - "The refactor is zero-output-change: the existing reporter unit tests and the report e2e still pass byte-for-byte"
    - "onHeal receives every heal event in raw mode: a healed event and each of the three post-scoring refusal reasons reach the callback, confirming OUT-01 already delivered by Phase 5"
    - "An uncaptured-locator (no-fingerprint) failure is correctly silent at onHeal, identical to fixture mode, documented as intended"
  artifacts:
    - path: "src/reporter/render.ts"
      provides: "Pure renderHealSummary(events: SelfmendEvent[]): string extracted from the reporter box logic"
      exports: ["renderHealSummary"]
    - path: "src/reporter/reporter.ts"
      provides: "Reporter refactored to call the shared renderHealSummary"
      contains: "renderHealSummary"
    - path: "src/reporter/render.test.ts"
      provides: "Byte-identical snapshot: same events to reporter path and to renderHealSummary, string equality"
      contains: "toBe"
    - path: "src/integration/onheal-confirm.test.ts"
      provides: "Confirming test that healed + the three refusal reasons reach onHeal; no-fingerprint stays silent"
      contains: "onHeal"
    - path: "src/index.ts"
      provides: "Public export of renderHealSummary"
      contains: "renderHealSummary"
  key_links:
    - from: "src/reporter/reporter.ts render()"
      to: "src/reporter/render.ts renderHealSummary"
      via: "render() returns renderHealSummary([...this.heals, ...this.refused])"
      pattern: "renderHealSummary"
    - from: "src/integration/wrap-page.ts emit"
      to: "onHeal callback"
      via: "fire-and-forget emit on both healed and refused arms"
      pattern: "onHeal"
---

<objective>
Expose the boxed heal summary renderer as a standalone pure function so a raw-framework user prints the exact same report from collected events without a Playwright reporter, and confirm the onHeal callback already delivers every heal event in raw mode. The byte-identical guarantee is the load-bearing constraint: the renderer must be ONE shared pure function the reporter also calls, not a copy that can drift.

This is the output vertical slice. After it, a developer collecting SelfmendEvents from an onHeal callback can call renderHealSummary(events) and get the identical boxed summary the @playwright/test reporter prints, closing the output gap for runner-agnostic mode.

Purpose: Deliver OUT-02 (shared byte-identical renderHealSummary) and confirm OUT-01 (onHeal receives healed + the three post-scoring refusals), the persistence slice's output counterpart.
Output: A pure src/reporter/render.ts exporting renderHealSummary(events), the reporter refactored to call it (zero output change), a byte-identical snapshot test, a raw-mode onHeal confirming test, and the public renderHealSummary export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-standalone-persistence-output/06-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase. Use these directly. -->

From src/reporter/reporter.ts (the box rendering to extract; currently instance methods reading this.heals / this.refused):
- render(): string                       // composes healed box + optional refused section, healed first
- private renderHealedBox(): string      // N=0 quiet line pc.dim("selfmend: 0 locators healed"); else boxed block
- private renderRefusedSection(): string | null  // null when no refusals; else yellow-header box
- private renderRow(h: HealEvent): string[]      // testName line + "<orig> -> <healed>  (score)"
- private renderRefusedRow(r: RefusedEvent): string[]  // testName line + "<orig> x <reason>  (best <score|dash>)"
- private boxLine(colored, plain, width): string // "| <colored><pad> |"
- Module helpers (currently in reporter.ts): formatScore(score) (toFixed(2)), stripAnsi(s) (EXPORTED), visibleLength(s)
- Box-drawing chars: top/bottom corners and horizontal rule, pc.bold/pc.cyan for healed header, pc.bold/pc.yellow for refused header
- The reporter keeps onBegin/onTestEnd/onEnd/mergeAndPersist unchanged; ONLY render() changes to delegate.

From src/integration/events.ts:
- type HealEvent = { kind?: "healed"; testName; originalSelector; healedTarget; score }
- type HealedEvent = HealEvent
- type RefusedReason = "no-candidates" | "below-floor" | "ambiguous"   // the three POST-SCORING reasons
- type RefusedEvent = { kind: "refused"; testName; originalSelector; reason: RefusedReason; bestScore: number | null }
- type SelfmendEvent = HealedEvent | RefusedEvent
  // A missing kind decodes as healed (events.ts:34). no-fingerprint is deliberately NOT a refused event (events.ts:50-56).

From src/integration/wrap-page.ts:
- WrapPageOptions.onHeal?: (event: SelfmendEvent) => void  // fire-and-forget, errors swallowed, no-op when absent
- emit forwards to onHeal on BOTH arms; locator-proxy emits refused (:414) and healed (:454).

From src/reporter/reporter.test.ts:
- The existing reporter unit tests import { stripAnsi } from "./reporter.js" and use resultWith / resultWithRaw helpers and a visible() ANSI stripper. Keep stripAnsi importable from reporter.js (re-export it from reporter.ts if render.ts becomes its new home) so these imports keep working.
</interfaces>

@src/reporter/reporter.ts
@src/reporter/reporter.test.ts
@src/integration/events.ts
@src/integration/wrap-page.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED then GREEN, byte-identical renderHealSummary extracted as the single shared renderer (OUT-02)</name>
  <files>src/reporter/render.test.ts, src/reporter/render.ts, src/reporter/reporter.ts</files>
  <read_first>
    - src/reporter/reporter.ts (the render/renderHealedBox/renderRefusedSection/renderRow/renderRefusedRow/boxLine methods and the formatScore/stripAnsi/visibleLength module helpers, the EXACT code to move; preserve healed-first then refused ordering)
    - src/reporter/reporter.test.ts (the existing reporter box-alignment + N=0 + two-section tests that must stay green; the resultWith/resultWithRaw helpers and the visible() stripper model)
    - src/integration/events.ts (SelfmendEvent union, the kind discriminant, missing-kind === healed rule)
  </read_first>
  <behavior>
    Byte-identical guarantee (the OUT-02 core):
    - Feed the SAME flat SelfmendEvent[] (a mix of healed and refused) to BOTH paths: a real SelfmendReporter instance (via its onTestEnd attachment path, exactly as reporter.test.ts builds attachments) and to renderHealSummary(events). Assert FULL STRING EQUALITY (toBe), not toContain. With a fixed color mode (force-color OR no-color) so the snapshot is deterministic, matching how the existing reporter tests handle color.
    - renderHealSummary partitions events into healed vs refused by kind (missing kind === healed), renders the healed box first then the refused section, each in input order. Healed-only input renders just the healed box (no trailing refused section). Refused-present input appends the yellow could-not-heal box.
    - N=0 (empty events) renders the single quiet dim line "selfmend: 0 locators healed" and no box, identical to the reporter's renderHealedBox N=0 branch.
    - A refused row with bestScore null renders the dash, never the literal "null"; singular/plural header ("1 locator healed" vs "N locators healed") matches the reporter exactly.
    - Box alignment with colored content stays aligned (width measured by stripping the full ANSI escape including the ESC byte), reproducing the existing WR-02 alignment behavior.
  </behavior>
  <action>
    RED first: write src/reporter/render.test.ts with the byte-identical equality test above plus the N=0, refused-section, plural-header, and null-bestScore-dash cases. Build the reporter side by feeding attachments through a SelfmendReporter exactly as reporter.test.ts does (resultWith / resultWithRaw style), call its render(), and assert toBe against renderHealSummary on the same events. Run and CONFIRM failure (renderHealSummary not yet present). Commit: test(06-02): add failing byte-identical renderHealSummary snapshot.

    GREEN: create src/reporter/render.ts exporting a PURE renderHealSummary(events: SelfmendEvent[]): string. MOVE (do not copy) the box logic out of reporter.ts: the healed-box composition, the refused-section composition, the per-row builders, boxLine, and the module helpers formatScore and visibleLength, plus the box-drawing characters and the pc color calls. Move stripAnsi here too but KEEP it importable from reporter.ts (re-export `export { stripAnsi } from "./render.js"` in reporter.ts) so existing reporter.test.ts imports keep working. The partition rule: missing kind === healed (events.ts:34). Preserve the EXACT healed-first then refused ordering and every space, color, and character.

    Refactor src/reporter/reporter.ts so render() becomes `return renderHealSummary([...this.heals, ...this.refused])` and delete the now-moved private methods/helpers. Leave onBegin/onTestEnd/onEnd/mergeAndPersist untouched. Run render.test.ts AND reporter.test.ts to GREEN. Commit: feat(06-02): extract shared pure renderHealSummary; reporter delegates (zero output change).

    Do NOT touch src/matching/. Do NOT change the box layout, colors, ordering, or any output character.
  </action>
  <verify>
    <automated>npx vitest run src/reporter/render.test.ts src/reporter/reporter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/reporter/render.ts exists and exports `function renderHealSummary(events: SelfmendEvent[]): string`.
    - reporter.ts render() body is `return renderHealSummary([...this.heals, ...this.refused])` (`grep -n "renderHealSummary" src/reporter/reporter.ts` shows the call).
    - The box-rendering private methods (renderHealedBox/renderRefusedSection/renderRow/renderRefusedRow/boxLine) no longer exist in reporter.ts (extracted, not copied): `grep -c "renderHealedBox" src/reporter/reporter.ts` returns 0.
    - render.test.ts asserts FULL string equality with `toBe` between the reporter's render() and renderHealSummary on identical events (`grep -n "toBe" src/reporter/render.test.ts` present).
    - `import { stripAnsi } from "./reporter.js"` still resolves (re-exported), so reporter.test.ts compiles unchanged.
    - `npx vitest run src/reporter/render.test.ts src/reporter/reporter.test.ts` exits 0.
    - No edit to any file under src/matching/.
  </acceptance_criteria>
  <done>OUT-02 delivered: renderHealSummary is the single shared pure renderer, proven byte-identical to the reporter via a string-equality snapshot, and the reporter delegates to it with zero output change.</done>
</task>

<task type="execute">
  <name>Task 2: Confirming raw-mode onHeal test (OUT-01) and export renderHealSummary</name>
  <files>src/integration/onheal-confirm.test.ts, src/index.ts</files>
  <read_first>
    - src/integration/emit-seam.test.ts (the existing model for unit-testing the emit seam without a Playwright runner)
    - src/integration/wrap-page.ts (the emit builder around lines 213-227: onHeal called fire-and-forget, throws swallowed; emit no-op when onHeal absent)
    - src/integration/events.ts (SelfmendEvent union; the three RefusedReason values; the documented no-fingerprint-is-silent rule at events.ts:50-56)
    - src/index.ts (the public exports surface; add renderHealSummary next to the existing event-type exports)
  </read_first>
  <action>
    Add src/integration/onheal-confirm.test.ts, a unit test (no Playwright runner) confirming OUT-01 is already delivered by Phase 5. Model it on src/integration/emit-seam.test.ts. Drive the emit seam (or a wrapPage with a recording onHeal) so that: a healed SelfmendEvent reaches onHeal, AND each of the three post-scoring refusal reasons (no-candidates, below-floor, ambiguous) reaches onHeal as a refused event. Assert the collected events match those expected kinds/reasons (mirror the reporter's RefusedReason set). Add an explicit assertion-or-comment documenting that an uncaptured-locator (no-fingerprint) failure does NOT reach onHeal by design (events.ts:50-56), so "every could-not-heal" reads correctly as "every post-scoring refusal," identical to fixture mode. If the test reveals any arm does not reach onHeal, that gap is the real OUT-01 work and must be fixed in wrap-page.ts / locator-proxy.ts; per the research code-read both arms already call emit, so expect no production change.

    In src/index.ts add `export { renderHealSummary } from "./reporter/render.js";` grouped with the existing heal-event transport exports, with a one-line comment noting it renders the standalone boxed summary from collected events (OUT-02), the output counterpart to onHeal (OUT-01).

    Do NOT touch src/matching/. Keep never-false-green untouched (it lives in the pure decide(), not exercised here).
  </action>
  <verify>
    <automated>npx vitest run src/integration/onheal-confirm.test.ts && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - src/integration/onheal-confirm.test.ts asserts a healed event AND all three refusal reasons (no-candidates, below-floor, ambiguous) reach onHeal (`grep -n "no-candidates\\|below-floor\\|ambiguous" src/integration/onheal-confirm.test.ts` shows all three).
    - The test documents (assertion or comment) that no-fingerprint stays silent at onHeal, consistent with fixture mode.
    - `grep -n "renderHealSummary" src/index.ts` shows the public export.
    - `npx vitest run` exits 0 (full unit suite green, including the existing 141 unit and the new tests).
    - No edit to any file under src/matching/.
  </acceptance_criteria>
  <done>OUT-01 confirmed in raw mode: healed plus the three post-scoring refusals reach onHeal and the intentional no-fingerprint silence is documented; renderHealSummary is publicly exported.</done>
</task>

<task type="execute">
  <name>Task 3: Full regression gate, prove zero behaviour change (existing 141 unit + 28 e2e)</name>
  <files>src/reporter/reporter.test.ts</files>
  <read_first>
    - src/reporter/reporter.test.ts (confirm its imports from reporter.js still resolve after the extraction; add a thin assertion only if the suite does not already cover the delegated render path)
    - tests/report.spec.ts (the existing reporter output e2e that must still pass byte-for-byte)
  </read_first>
  <action>
    Run the full unit suite and the full Playwright e2e suite to prove the OUT-02 extraction caused zero behaviour change (the WRAP-04-style hard gate). If reporter.test.ts does not already exercise render() after delegation, add one small assertion that the reporter's render() output for a representative healed+refused event mix is unchanged (snapshot or exact-string against the known box), without weakening any existing assertion. Do not modify production code in this task unless a regression is found; if a regression appears, fix it in render.ts (the single renderer) so the reporter and the public function stay byte-identical, never by re-copying logic.

    Do NOT touch src/matching/.
  </action>
  <verify>
    <automated>npx vitest run && npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 with no fewer passing unit tests than before this plan (no regression; existing 141 unit plus the new render/onheal tests all green).
    - `npx playwright test` exits 0 including tests/report.spec.ts (reporter output byte-unchanged) and the standalone specs.
    - No existing assertion in reporter.test.ts was weakened or removed.
    - No edit to any file under src/matching/.
  </acceptance_criteria>
  <done>Zero-behaviour-change proven: the renderer extraction leaves the full unit suite and the reporter output e2e green, satisfying the WRAP-04-style hard gate for OUT-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| collected events -> renderHealSummary | A consumer passes a flat SelfmendEvent[] (possibly with missing/extra fields) to the pure renderer |
| heal/refuse decision -> onHeal callback | Heal events cross from the locator proxy to a consumer-supplied callback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering | renderHealSummary fed a malformed event (missing kind / null bestScore) | mitigate | The partition treats missing kind as healed (events.ts:34) and null bestScore renders a dash, never "null"; the pure renderer never throws on a well-typed SelfmendEvent and is covered by the null-bestScore test |
| T-06-07 | Denial of service | a throwing onHeal callback stalling or breaking the run | mitigate | emit is fire-and-forget with throws and rejected promises swallowed (wrap-page.ts:213-227); the confirming test exercises the recorded path, the existing fail-safe controls remain in tests/wrap-page.spec.ts |
| T-06-08 | Information disclosure | the rendered summary leaking raw DOM | mitigate | Events carry derived audit fields only (selectors, score, reason); the renderer prints only those, no raw DOM, unchanged from the reporter |
| T-06-09 | Tampering | renderer drift breaking byte-identity (a space/color/order difference) | mitigate | Single shared pure function; reporter calls it; the byte-identical snapshot asserts full string equality, and a toContain-passes-but-equality-fails warning sign is covered by toBe |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | This phase installs NO packages (source-only addition; runtime deps stay zod + picocolors). Package Legitimacy Gate N/A |
</threat_model>

<verification>
- `npx vitest run` exits 0 (full unit suite green; existing 141 unit plus render.test.ts and onheal-confirm.test.ts).
- `npx playwright test` exits 0 (existing 28 e2e including tests/report.spec.ts byte-unchanged).
- The byte-identical snapshot asserts toBe equality between the reporter's render() and renderHealSummary for the same events.
- renderHealedBox/renderRefusedSection/renderRow/renderRefusedRow/boxLine no longer exist in reporter.ts (extracted, not copied).
- renderHealSummary is exported from src/index.ts.
- No file under src/matching/ is modified. No em dashes in any new source or test prose.
</verification>

<success_criteria>
- OUT-02: renderHealSummary(events) renders the boxed summary byte-identical to the reporter for the same events, proven by a string-equality snapshot, and the reporter delegates to the single shared pure function (no copy).
- OUT-01: onHeal receives every heal event in raw mode (healed plus the three post-scoring refusals), confirmed by a test; the intentional no-fingerprint silence is documented.
- Zero behaviour change: the existing reporter unit tests and report e2e stay green byte-for-byte.
- renderHealSummary is publicly exported from src/index.ts.
- Pure matching core untouched; no store-format or version change; no new runtime dependency.
</success_criteria>

<output>
Create `.planning/phases/06-standalone-persistence-output/06-02-SUMMARY.md` when done.
</output>
