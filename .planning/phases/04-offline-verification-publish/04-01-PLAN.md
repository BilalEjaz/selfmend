---
phase: 04-offline-verification-publish
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - package-lock.json
  - tests/offline.spec.ts
  - .gitattributes
  - .github/workflows/nul-guard.yml
autonomous: true
requirements: [PRIV-01]
user_setup: []

must_haves:
  truths:
    - "A committed package-lock.json lets a clean `npm ci` install reproducibly (D-08)"
    - "A full capture+heal cycle completes with zero Node-process network egress (PRIV-01, D-03)"
    - "The offline test is self-validating: it FAILS if any external connection is attempted, so it can never silently pass"
    - "The source layer (src/) imports no node network module — offline by construction (D-04)"
    - "Tracked source files are guarded against raw NUL bytes (D-07 discretion, two Phase 3 incidents)"
  artifacts:
    - path: "package-lock.json"
      provides: "Reproducible install contract (lockfileVersion 3, @types/node 24.12.4, @playwright/test 1.60.0)"
      contains: "lockfileVersion"
    - path: "tests/offline.spec.ts"
      provides: "PRIV-01 in-process network-block heal-cycle test"
      min_lines: 60
    - path: ".gitattributes"
      provides: "Text normalization to surface NUL-byte corruption"
    - path: ".github/workflows/nul-guard.yml"
      provides: "CI guard that fails on a NUL byte in tracked source"
  key_links:
    - from: "tests/offline.spec.ts"
      to: "src/integration/fixture.ts"
      via: "healingFixture import + real page heal cycle"
      pattern: "healingFixture"
    - from: "tests/offline.spec.ts"
      to: "egress counter"
      via: "stub net/http/https/dns/tls/fetch to count+throw, assert counter === 0"
      pattern: "counter"
---

<objective>
Land the two gating prerequisites for the publish: commit the regenerated lockfile (D-08, unblocks `npm ci` everywhere) and prove PRIV-01 with a self-validating in-process network-block heal-cycle test (D-03). Also add the cheap NUL-byte guard recommended after two Phase 3 incidents.

Purpose: PRIV-01 is the last open v1 requirement and the product's headline security property — the offline guarantee must be proven mechanically, not asserted. The lockfile must exist before any pack/publish/CI prep so installs are reproducible.

Output: a committed `package-lock.json`, `tests/offline.spec.ts` (the canonical PRIV-01 proof), `.gitattributes` + a NUL-guard CI workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-offline-verification-publish/04-RESEARCH.md
@.planning/phases/04-offline-verification-publish/04-CONTEXT.md

<interfaces>
<!-- The heal path the offline test must exercise. Extracted from the repo — use directly, no exploration needed. -->

From src/integration/fixture.ts:
  export const healingFixture  // test.extend mixin; overrides `page` so every page.locator(...) is healing-aware

From src/integration/events.ts:
  export const HEAL_ATTACHMENT_NAME  // attachment name a successful heal writes (used to assert a heal fired)

Model the test after tests/heal.spec.ts (already green):
  - INDEX_URL  = file:// tests/fixture-app/index.html   (capture page)
  - BROKEN_URL = file:// tests/fixture-app/broken.html   (".btn-primary" renamed to ".btn-cta"; identity survives)
  - flow: goto INDEX_URL → page.locator(".btn-primary").waitFor() (capture) → goto BROKEN_URL →
          same locator .click({ timeout: 1200 }) heals after the real timeout → GREEN

VERIFIED in research (instrumented this repo): a real Chromium launch+page+click+evaluate makes ZERO calls to
net.connect / net.Socket.connect / http.request / https.request / dns.lookup|resolve* / tls.connect / fetch.
Chromium's CDP transport is a child_process.spawn + stdio pipe, NOT a Node TCP socket. A blanket throw-on-egress
stub therefore does NOT break the browser. No loopback allowlist required.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the regenerated package-lock.json (D-08)</name>
  <files>package-lock.json</files>
  <read_first>
    - .planning/phases/04-offline-verification-publish/04-RESEARCH.md (Pitfall 2 + "Verified clean-install recovery (D-08)" — the resolver-crash root cause and the verified fix)
    - .planning/phases/03-persistence-parallel-worker-safety/03-02-SUMMARY.md (Deviation 1: the @types/node hand-install that left the lockfile stale)
    - .planning/phases/04-offline-verification-publish/04-CONTEXT.md (D-08)
  </read_first>
  <action>The regenerated `package-lock.json` is already present in the repo as an untracked file (research ran a clean `rm -rf node_modules && npm install`, exit 0, producing lockfileVersion 3 with @types/node 24.12.4 and @playwright/test 1.60.0). Stage and commit ONLY this file (do NOT hand-edit it — a hand-edited tree re-triggers the arborist `Cannot read properties of null (reading 'children')` crash per D-08/Pitfall 2). Before committing, run a clean install from the committed state to confirm reproducibility: `npm ci` must exit 0. If `npm ci` or a fresh `npm install` fails with the arborist null/children error, that is a blocker to DIAGNOSE per D-08 (root cause is a node_modules tree with no hidden lockfile) — recover via `rm -rf node_modules && npm install`, never paper over it. Commit message per the project commit convention.</action>
  <verify>
    <automated>npm ci 2>&1 | Select-String -Pattern "added|up to date"; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - `package-lock.json` is tracked by git (appears in `git ls-files`, not in `git status --short` as `??`)
    - `package-lock.json` contains `"lockfileVersion": 3`
    - `npm ci` exits 0 against the committed lockfile (reproducible install proven)
    - The lockfile was NOT hand-edited (it is the verbatim `npm install` output)
  </acceptance_criteria>
  <done>package-lock.json is committed and `npm ci` installs cleanly from it (exit 0).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PRIV-01 self-validating offline network-block heal-cycle test (D-03)</name>
  <files>tests/offline.spec.ts</files>
  <read_first>
    - tests/heal.spec.ts (the model: capture on index.html, heal on broken.html, the HEAL_ATTACHMENT_NAME assertion)
    - src/integration/fixture.ts (healingFixture — the real page-override heal path the test must run)
    - .planning/phases/04-offline-verification-publish/04-RESEARCH.md (Pattern 1 + the verified egress-block code shape + Pitfall 1: scope the block per-test, NEVER globalSetup)
    - .planning/phases/04-offline-verification-publish/04-CONTEXT.md (D-03, D-04)
  </read_first>
  <behavior>
    - SELF-VALIDATION (must come first, proves the harness genuinely trips): with the egress block installed, attempt a real outbound connection (e.g. call the stubbed `fetch` or `net.connect` directly) and assert it throws an OfflineViolationError AND increments the egress counter to a non-zero value. This proves the test cannot silently pass — if the block were a no-op, this sub-assertion fails.
    - HEAL UNDER BLOCK (the load-bearing PRIV-01 assertion): with the egress block installed, run the full capture+heal cycle through `healingFixture` (goto index.html → waitFor capture → goto broken.html → click heals after real timeout → GREEN, identical to heal.spec.ts), then assert the egress counter === 0 — the entire offline heal path attempted zero outbound connections.
    - A heal event was attached (HEAL_ATTACHMENT_NAME present, score >= 0.9) so the cycle genuinely healed and did not merely no-op.
    - afterEach restores all patched originals so the block never leaks to other tests.
  </behavior>
  <action>Create `tests/offline.spec.ts` modeled on `tests/heal.spec.ts`, importing `healingFixture as test` and the fixture-app file:// URLs. Implement `installEgressBlock(counter)` that patches `net.connect`, `net.Socket.prototype.connect`, `http.request`, `https.request`, `dns.lookup`, `dns.promises.lookup`, `tls.connect`, and `globalThis.fetch` to increment `counter.n` and throw an `OfflineViolationError`, returning a `restore()` that reverts every patch. Install per-test in a `beforeEach`/`try` and restore in a `finally`/`afterEach` — NEVER in globalSetup (Pitfall 1: globalSetup would also block the CI browser download). Keep this as ONE PRIV-01 heal-cycle test plus a separate assertion that the source layer imports no network module (covered in Task 3's static guard, do not duplicate the merge path here per research Open Q1 — the heal cycle is the canonical proof and persistence.ts is proven network-free statically). Use the throw-on-ANY variant (verified safe for Chromium); a loopback allowlist is not required. No fenced code in this file beyond what an executor writes; follow the heal.spec.ts structure for URL resolution (fileURLToPath/pathToFileURL).</action>
  <verify>
    <automated>npx playwright test tests/offline.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/offline.spec.ts` exits 0 (test passes)
    - The test contains a self-validation step that asserts the egress counter TRIPS (becomes non-zero) and throws when an outbound connection is attempted — proving it is not a silent pass
    - The PRIV-01 assertion is `expect(counter.n).toBe(0)` after a full GREEN capture+heal cycle
    - Stubs are installed per-test (beforeEach/in-test), NOT in globalSetup or a global file
    - A heal event (HEAL_ATTACHMENT_NAME) with score >= 0.9 is asserted, so the cycle genuinely healed offline
  </acceptance_criteria>
  <done>The PRIV-01 offline heal-cycle test passes, is self-validating (trips on a real connection attempt), and asserts zero egress across a genuine heal.</done>
</task>

<task type="auto">
  <name>Task 3: NUL-byte guard — .gitattributes + CI grep guard + source static-network assertion (D-07 discretion)</name>
  <files>.gitattributes, .github/workflows/nul-guard.yml</files>
  <read_first>
    - .planning/phases/03-persistence-parallel-worker-safety/03-02-SUMMARY.md (Deviation 2: raw NUL bytes in locator-proxy.ts + merge.ts shipped as binary; the two incidents motivating this guard)
    - .planning/phases/04-offline-verification-publish/04-RESEARCH.md (Security Domain: NUL-byte tracking guard recommended; Validation Architecture Wave 0 — static "no network import in src" assertion)
    - .planning/phases/04-offline-verification-publish/04-CONTEXT.md (D-07 Claude's discretion: recommended given two stray-NUL incidents)
  </read_first>
  <action>Add `.gitattributes` declaring `*.ts text` (and `*.json text`, `*.md text`) so git treats source as text and surfaces accidental binary/NUL corruption in diffs. Add `.github/workflows/nul-guard.yml`: a tiny standalone GitHub Actions workflow on push/PR that checks out the repo and fails if any tracked `src/**/*.ts` or `tests/**/*.ts` file contains a NUL byte (use a portable grep for the NUL byte over tracked files; exit non-zero on any hit). In the SAME guard job, add a static offline-by-construction assertion (D-04): fail if `src/` contains any import of `node:http`, `node:https`, `node:net`, `node:dns`, or `node:tls` — research VERIFIED zero such imports today, so this guards against regression. Filter out comment lines before counting so header prose cannot self-invalidate the gate (use a `grep -v '^#'`-style filter, never a bare `== 0` on the unfiltered file). Keep this workflow separate from the main matrix CI (Plan 03) so it stays a fast, focused integrity gate.</action>
  <verify>
    <automated>git grep -Il $([char]0) -- 'src/**/*.ts' 'tests/**/*.ts'; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }</automated>
  </verify>
  <acceptance_criteria>
    - `.gitattributes` exists declaring `*.ts text` (at minimum)
    - `.github/workflows/nul-guard.yml` exists, triggers on push + pull_request, and fails the job if a NUL byte appears in any tracked `src/`/`tests/` `.ts` file
    - The same workflow statically asserts `src/` imports none of `node:http|https|net|dns|tls` (D-04 offline-by-construction regression guard), with comment lines filtered out before the count
    - A local grep over tracked `.ts` source finds ZERO NUL bytes (the guard passes on the current clean tree)
  </acceptance_criteria>
  <done>The NUL-byte + no-network-import guard workflow exists and passes on the current clean source; .gitattributes normalizes source as text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| heal path → network | The plugin must never open an outbound connection during capture/heal (the headline privacy guarantee) |
| developer → tracked source | Hand-extracted deps / raw NUL bytes can corrupt the tree (two Phase 3 incidents) |
| consumer/CI → install | `package-lock.json` is the integrity-pinned install contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | heal path Node-process egress | mitigate | PRIV-01 self-validating throw-on-egress test (Task 2): counter trips on a real connection, stays 0 across a genuine heal |
| T-04-02 | Tampering / Integrity | package-lock.json | mitigate | Commit the verbatim `npm install` lockfile; prove `npm ci` exits 0; never hand-edit (Task 1, D-08) |
| T-04-03 | Tampering / Integrity | tracked src/tests source | mitigate | `.gitattributes` text normalization + CI NUL-byte guard (Task 3, two Phase 3 incidents) |
| T-04-04 | Information Disclosure | future src/ network import regression | mitigate | Static CI assertion that src/ imports no node:http/https/net/dns/tls (Task 3, D-04) |
</threat_model>

<verification>
- `npm ci` exits 0 from the committed lockfile.
- `npx playwright test tests/offline.spec.ts` passes and is self-validating.
- The NUL-guard workflow passes on the clean tree; no NUL bytes in tracked `.ts`.
- `npm test` (vitest unit) and the existing `npm run test:e2e` remain green (no regression).
</verification>

<success_criteria>
- PRIV-01 is proven: a full offline capture+heal cycle attempts zero network egress, with a test that genuinely fails if egress occurs.
- The lockfile is committed and installs reproducibly via `npm ci`.
- Source is guarded against NUL-byte corruption and against new network imports.
</success_criteria>

<output>
Create `.planning/phases/04-offline-verification-publish/04-01-SUMMARY.md` when done
</output>
