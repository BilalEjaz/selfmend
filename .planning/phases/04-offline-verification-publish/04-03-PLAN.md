---
phase: 04-offline-verification-publish
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - .github/workflows/ci.yml
  - RELEASING.md
autonomous: false
requirements: [PRIV-01]
user_setup: []

must_haves:
  truths:
    - "A GitHub Actions matrix proves the suite on node 22/24 × @playwright/test 1.42/1.60 (D-05)"
    - "CI installs from the committed lockfile via npm ci and runs lint+unit+integration+offline test+pack+publint+attw (D-05)"
    - "CI holds NO NPM_TOKEN and performs no auto-publish — release is manual (D-06)"
    - "The terminal phase gate is a green `npm publish --dry-run` (+ pack + publint + attw), NOT a real publish (D-01)"
    - "A RELEASING checklist documents the human publish step (npm login, --access public, dist-tag, 2FA, tag) (D-01)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "matrix CI (compatibility + regression net), manual release only"
      contains: "matrix"
    - path: "RELEASING.md"
      provides: "the human npm publish checklist"
      contains: "npm publish --access public"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "@playwright/test floor + latest"
      via: "npm install --no-save @playwright/test@${{ matrix.playwright }} after npm ci"
      pattern: "--no-save @playwright/test"
    - from: "terminal proof"
      to: "publish readiness"
      via: "npm ci && build && publint && attw && npm pack --dry-run && npm publish --dry-run all green"
      pattern: "publish --dry-run"
---

<objective>
Wire the compatibility-matrix CI (D-05), run the terminal publish-readiness proof (D-01), and write the human publish checklist (RELEASING.md). This is the final gate of the final phase: prove the package installs cleanly across the supported Playwright range and is publish-ready, then stop just short of the irreversible publish.

Purpose: the matrix is both the regression net and the cross-version compatibility proof (success criteria 2 + 3). The dry-run proof + RELEASING checklist deliver a publish-ready package while keeping the irreversible `npm publish` a documented human step (D-01/D-06).

Output: `.github/workflows/ci.yml`, `RELEASING.md`, and a verified green `npm publish --dry-run`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-offline-verification-publish/04-RESEARCH.md
@.planning/phases/04-offline-verification-publish/04-CONTEXT.md
@package.json

<interfaces>
<!-- VERIFIED terminal-proof sequence (research ran all green this session; selfmend@0.0.0 then, 0.1.0 now): -->
  npm ci                              # clean install from committed lockfile
  npm run build                       # tsdown → dist (exit 0)
  npx publint                         # "All good!"
  npx @arethetypeswrong/cli --pack    # exit 0 (node10 subpath 💀 is EXPECTED + ignorable, not a failure — Pitfall 4)
  npm pack --dry-run                  # ~20 files, dist+README+package.json
  npm publish --dry-run               # "+ selfmend@0.1.0"  (NO real publish, D-01)

<!-- VERIFIED matrix mechanics (research): @playwright/test is a peerDependency, so per-leg version override is safe: -->
  - run: npm ci
  - run: npm install --no-save @playwright/test@${{ matrix.playwright }}   # --no-save: no lockfile/package.json rewrite
  - run: npx playwright install --with-deps chromium
  matrix: os=[ubuntu-latest], node=[22,24], playwright=["1.42.0","1.60.0"]
  pack/publint/attw need run on only ONE leg (e.g. node==24 && playwright=='1.60.0') to cut noise — Claude's discretion.
  Honest-floor rule (Open Q2): if the 1.42 leg fails the suite, RAISE the declared peerDependencies floor to the
  lowest version that passes and update README/package.json — do NOT claim false support.

<!-- npm scripts CI composes (already present): test (vitest), test:e2e (playwright incl. tests/offline.spec.ts), -->
<!-- typecheck (tsc --noEmit), build (tsdown), lint:pack (publint), lint:types (attw --pack). -->

<!-- D-06: NO NPM_TOKEN in CI, no auto-publish. GitHub Actions pinned to @v4 (first-party actions/checkout, actions/setup-node). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitHub Actions compatibility-matrix CI (D-05, manual release D-06)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/04-offline-verification-publish/04-RESEARCH.md ("GitHub Actions matrix (D-05)" recommended shape; Pattern 3 peer-dep override; Pitfall 4 node10 attw; Open Q2 honest-floor rule)
    - .planning/phases/04-offline-verification-publish/04-CONTEXT.md (D-05, D-06)
    - package.json (the scripts CI composes + the peerDependencies floor >=1.42)
  </read_first>
  <action>Create `.github/workflows/ci.yml` triggered on push + pull_request. Define a `test` job with a matrix: `os: [ubuntu-latest]`, `node: [22, 24]`, `playwright: ["1.42.0", "1.60.0"]`, `fail-fast: false`. Steps: `actions/checkout@v4`; `actions/setup-node@v4` with `node-version: ${{ matrix.node }}` and `cache: npm`; `npm ci` (installs from the committed lockfile); `npm install --no-save @playwright/test@${{ matrix.playwright }}` (per-leg peer-dep override, safe because it is a peerDependency — Pattern 3); `npx playwright install --with-deps chromium`; `npm run typecheck`; `npm test` (vitest unit); `npm run test:e2e` (Playwright integration — this INCLUDES tests/offline.spec.ts, the PRIV-01 proof); `npm run build`. Then gate the publish-surface validation steps to ONE leg (e.g. `if: matrix.node == 24 && matrix.playwright == '1.60.0'`): `npx publint`, `npx @arethetypeswrong/cli --pack`, `npm pack --dry-run`. Do NOT add an NPM_TOKEN secret, a publish step, or any release automation (D-06 — manual release only). Pin both actions to `@v4`. Document inline that the node10 attw 💀 on the `./reporter` subpath is expected (Pitfall 4) and that if the 1.42 leg fails, the declared floor is raised per the honest-floor rule (Open Q2) rather than claiming false support. This is a separate workflow from the Plan 01 nul-guard.yml.</action>
  <verify>
    <automated>npx --yes js-yaml .github/workflows/ci.yml > $null; if ($LASTEXITCODE -ne 0) { exit 1 }; $ok = (Select-String -Path .github/workflows/ci.yml -Pattern "1.42.0" -Quiet) -and (Select-String -Path .github/workflows/ci.yml -Pattern "npm ci" -Quiet) -and (-not (Select-String -Path .github/workflows/ci.yml -Pattern "NPM_TOKEN" -Quiet)); if (-not $ok) { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` is valid YAML, triggers on push + pull_request
    - Matrix is `node: [22, 24]` × `playwright: ["1.42.0", "1.60.0"]` with `fail-fast: false`
    - Steps run `npm ci`, the `--no-save @playwright/test@${{ matrix.playwright }}` override, playwright browser install, typecheck, vitest, `npm run test:e2e` (incl. the offline test), and build
    - publint + attw + `npm pack --dry-run` run on a single gated leg
    - The workflow contains NO `NPM_TOKEN` and NO publish step (D-06)
    - Actions are pinned to `@v4`
  </acceptance_criteria>
  <done>The matrix CI workflow exists, is valid YAML, runs the full suite incl. the offline test across node 22/24 × PW 1.42/1.60, and never publishes or holds a token.</done>
</task>

<task type="auto">
  <name>Task 2: Run the terminal publish-readiness proof and write RELEASING.md (D-01)</name>
  <files>RELEASING.md</files>
  <read_first>
    - .planning/phases/04-offline-verification-publish/04-RESEARCH.md ("Verified terminal proof sequence (D-01)" + "Human publish checklist"; Pitfall 4 node10 attw expected)
    - .planning/phases/04-offline-verification-publish/04-CONTEXT.md (D-01, D-06)
    - package.json (version must read 0.1.0 from Plan 02; prepublishOnly must exist)
  </read_first>
  <action>Run the VERIFIED terminal-proof sequence locally and confirm all green WITHOUT publishing: `npm ci`, `npm run build`, `npx publint` (expect "All good!"), `npx @arethetypeswrong/cli --pack` (exit 0; the node10 `./reporter` 💀 is EXPECTED and ignorable per Pitfall 4 — only a non-node10 row turning bad is a real failure), `npm pack --dry-run` (dist + README + package.json only, no source/maps/secrets), and finally `npm publish --dry-run` (must print `+ selfmend@0.1.0` and NOT publish). DO NOT run the real `npm publish` (D-01 — irreversible, needs the human's npm auth). Inspect the `npm pack --dry-run` file list and confirm the tarball contains ONLY `dist/`, `README.md`, `package.json` (no `.env`, no `.selfmend`, no `src/`, no tests, no `.map`). Then write `RELEASING.md` documenting the human publish step: `npm login` (interactive, 2FA), `npm whoami` (confirm account), `npm publish --access public` (prepublishOnly re-guards the build), `git tag v0.1.0 && git push --tags`. State explicitly that CI does not publish (D-06) and that the maintainer runs these manually. No fenced-code requirement for the plan; the executor writes the commands into RELEASING.md.</action>
  <verify>
    <automated>npm ci; npm run build; npx publint; npx @arethetypeswrong/cli --pack; npm pack --dry-run; npm publish --dry-run 2>&1 | Select-String -Pattern "selfmend@0.1.0" -Quiet; if (-not $?) { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - `npm publish --dry-run` exits 0 and reports `selfmend@0.1.0` (NO real publish performed)
    - `npx publint` reports all good; `npx attw --pack` exits 0 (only the expected node10 subpath 💀)
    - `npm pack --dry-run` tarball contains ONLY dist/, README.md, package.json — no .env, .selfmend, src/, tests, or .map files
    - `RELEASING.md` documents the human publish checklist (npm login + 2FA, whoami, `npm publish --access public`, git tag) and states release is manual / CI holds no token (D-06)
  </acceptance_criteria>
  <done>The terminal proof is green (publish --dry-run reports selfmend@0.1.0, tarball is dist-only), and RELEASING.md documents the manual human publish.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full Phase 4 deliverable: PRIV-01 offline test (Plan 01), committed lockfile, 0.1.0 + prepublishOnly + CHANGELOG + D-07 README + no source maps (Plan 02), the matrix CI workflow + green terminal dry-run proof + RELEASING.md (this plan). The package is publish-ready; the real `npm publish` is intentionally NOT run (D-01).</what-built>
  <how-to-verify>
    1. Confirm the terminal proof was green: re-run `npm ci && npm run build && npx publint && npx @arethetypeswrong/cli --pack && npm pack --dry-run && npm publish --dry-run` — the last line should read `+ selfmend@0.1.0` and nothing should be published.
    2. Inspect the `npm pack --dry-run` file list: it must list ONLY `dist/*`, `README.md`, `package.json` (no source, tests, .env, .selfmend, or .map files).
    3. Confirm `npx playwright test tests/offline.spec.ts` passes (PRIV-01 proven offline).
    4. Skim `README.md` for the config table (enabled/threshold/margin/testIdAttr + SELFMEND_PRUNE), the trust-model section, the committed-baseline workflow, and the Limitations section.
    5. Confirm `.github/workflows/ci.yml` holds no NPM_TOKEN and has no publish step, and review whether you want the optional `windows-latest` matrix leg (the atomicWrite path is Windows-specific) before pushing — Claude scoped it to ubuntu-latest per research; add it if you want Windows coverage in CI.
    6. When ready to actually release (out of this phase's scope), follow `RELEASING.md`.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 4 complete, or describe any changes (e.g. "add windows-latest leg", "raise the floor", "reword limitations").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → npm registry | CI must never hold publish rights or auto-publish (manual release only) |
| published tarball → consumer | The tarball must contain only dist (no secrets, source, or maps) |
| declared peer range → reality | The package must not claim Playwright support it has not proven (matrix is the proof) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Elevation of Privilege | CI publish rights | mitigate | No NPM_TOKEN, no publish step in CI; release is manual (Task 1, D-06) |
| T-04-09 | Information Disclosure | published tarball contents | mitigate | `npm pack --dry-run` inspection confirms dist-only tarball (Task 2, files:[dist]) |
| T-04-10 | Spoofing | false Playwright-version support claim | mitigate | Matrix proves 1.42 + 1.60; honest-floor rule raises the declared floor if 1.42 fails (Task 1, Open Q2) |
| T-04-11 | Tampering | stale dist on manual publish | mitigate | prepublishOnly (Plan 02) re-guards; terminal dry-run confirms the surface before the human publishes (Task 2) |
</threat_model>

<verification>
- `.github/workflows/ci.yml` is valid YAML, has the node×playwright matrix, runs the full suite incl. the offline test, and holds no NPM_TOKEN.
- `npm publish --dry-run` reports `selfmend@0.1.0` with a dist-only tarball; publint + attw green.
- `RELEASING.md` documents the manual human publish.
- The human checkpoint approves the publish-ready state (no real publish performed).
</verification>

<success_criteria>
- A matrix CI proves compatibility (node 22/24 × PW 1.42/1.60) and acts as the regression net, with no publish rights.
- The package is dry-run publish-ready as selfmend@0.1.0 with a dist-only tarball, and the human publish step is documented.
- Phase 4 (and the v1 milestone) is complete pending the human's out-of-band `npm publish`.
</success_criteria>

<output>
Create `.planning/phases/04-offline-verification-publish/04-03-SUMMARY.md` when done
</output>
