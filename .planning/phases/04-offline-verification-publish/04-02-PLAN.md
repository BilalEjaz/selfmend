---
phase: 04-offline-verification-publish
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - package.json
  - tsdown.config.ts
  - CHANGELOG.md
  - README.md
autonomous: true
requirements: [PRIV-01]
user_setup: []

must_haves:
  truths:
    - "The package version is 0.1.0, the first pre-1.0 release (D-02)"
    - "A CHANGELOG.md records the 0.1.0 entry (D-02)"
    - "A prepublishOnly script rebuilds + re-validates so a manual publish cannot ship stale/unbuilt dist (D-01)"
    - "The README documents zero-friction install, verified config defaults, the never-false-green trust model, the committed-baseline workflow, and honest limitations (D-07)"
    - "The published build ships no source maps that reference un-shipped src/ (Pitfall 5)"
  artifacts:
    - path: "package.json"
      provides: "version 0.1.0 + prepublishOnly guard"
      contains: "prepublishOnly"
    - path: "CHANGELOG.md"
      provides: "0.1.0 release notes"
      contains: "0.1.0"
    - path: "README.md"
      provides: "launch docs (D-07)"
      contains: "SELFMEND_PRUNE"
    - path: "tsdown.config.ts"
      provides: "publish build with sourcemap disabled"
      contains: "sourcemap"
  key_links:
    - from: "package.json prepublishOnly"
      to: "build + lint:pack + lint:types"
      via: "npm run build && publint && attw"
      pattern: "prepublishOnly"
    - from: "README.md config table"
      to: "src/config/schema.ts defaults"
      via: "documented defaults match the zod schema (enabled true, threshold 0.9, margin 0.05, testIdAttr data-testid)"
      pattern: "0.9"
---

<objective>
Bring the package to a publish-ready, documented state: bump to 0.1.0, write the CHANGELOG, expand the README per D-07, add the `prepublishOnly` build-and-validate guard, and stop shipping dead source maps. This is the publish-prep layer that the terminal dry-run proof (Plan 03) verifies.

Purpose: a manual human publish must be safe (no stale dist) and the README must let a first-time adopter install in one line and trust the never-false-green guarantee. Version + changelog make 0.1.0 a real, semver-honest first release.

Output: package.json (0.1.0 + prepublishOnly), CHANGELOG.md, an expanded README.md, tsdown.config.ts with `sourcemap: false`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-offline-verification-publish/04-RESEARCH.md
@.planning/phases/04-offline-verification-publish/04-CONTEXT.md
@README.md
@package.json
@tsdown.config.ts

<interfaces>
<!-- VERIFIED config defaults (read straight from src/config/schema.ts) — the README config table MUST match these exactly. -->
  enabled    : true            (master switch, CFG-01)
  threshold  : 0.9             (confidence floor, D-09)
  margin     : 0.05            (second-best absolute gap, MATCH-03 / D-01)
  testIdAttr : "data-testid"   (test-id signal attribute)
  env SELFMEND_PRUNE           (opt-in destructive prune of orphaned baselines; off by default)

<!-- Existing package.json scripts the prepublishOnly guard composes (do not invent new ones): -->
  build      : tsdown
  lint:pack  : publint
  lint:types : attw --pack

<!-- Accepted Phase 3 limitations to document honestly in README (from 03-REVIEW.md): -->
  WR-03: occurrence index is counted per chained-method invocation, so divergent control flow
         between capture and heal runs degrades to no-heal (fail-safe, never a mis-heal).
  WR-04: selectOption/setInputFiles value-object payloads on the replay path (latent, currently tolerated).

<!-- Committed-baseline workflow (from .gitignore, already in place): -->
  .selfmend/baseline.json is COMMITTED (heals persist + are diffable in review).
  .selfmend/shards/ and .selfmend/*.tmp are gitignored (transient per-worker output).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump to 0.1.0, add prepublishOnly guard, disable publish source maps</name>
  <files>package.json, tsdown.config.ts</files>
  <read_first>
    - package.json (current version 0.0.0; existing scripts build / lint:pack / lint:types)
    - tsdown.config.ts (current build config; source maps currently emitted)
    - .planning/phases/04-offline-verification-publish/04-RESEARCH.md (Pattern 2 prepublishOnly; Pitfall 5 source maps reference un-shipped src/)
    - .planning/phases/04-offline-verification-publish/04-CONTEXT.md (D-01, D-02)
  </read_first>
  <action>In `package.json`: set `version` from `0.0.0` to `0.1.0` (D-02, first pre-1.0 release). Add a `prepublishOnly` script that runs `npm run build` then `npm run lint:pack` (publint) then `npm run lint:types` (attw --pack), so a manual `npm publish` cannot ship stale/unbuilt dist or a regressed exports surface (D-01, Pattern 2 — npm runs prepublishOnly on publish, NOT on install). Do NOT add any runtime dependency (offline guarantee, D-04 — keep deps zod + picocolors only). In `tsdown.config.ts`: set `sourcemap: false` for the published build (Pitfall 5 — the ~130 kB of `.map` files reference `src/` that `files:[dist]` does not ship, so they are dead weight and a minor path leak; publint/attw/pack stay green without them). Keep `files:[dist]` and the dual exports map untouched (already valid).</action>
  <verify>
    <automated>npm run build; node -e "const p=require('./package.json'); if(p.version!=='0.1.0'){console.error('version not 0.1.0');process.exit(1)}; if(!p.scripts.prepublishOnly){console.error('no prepublishOnly');process.exit(1)}; console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `version` === `0.1.0`
    - `package.json` `scripts.prepublishOnly` runs build then publint then attw
    - No new entries under `dependencies` (still only zod + picocolors)
    - `tsdown.config.ts` sets `sourcemap: false`; after `npm run build`, `dist/` contains no `.map` files
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Version is 0.1.0, prepublishOnly guards the publish, and the built dist ships no source maps.</done>
</task>

<task type="auto">
  <name>Task 2: Write CHANGELOG.md (0.1.0 entry)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the v1 capability set this release delivers)
    - .planning/ROADMAP.md (Phases 1-4 — what 0.1.0 ships)
    - .planning/phases/04-offline-verification-publish/04-CONTEXT.md (D-02; CHANGELOG format is Claude's discretion)
  </read_first>
  <action>Create `CHANGELOG.md` in keep-a-changelog format with a single `## [0.1.0]` entry dated 2026-05-31 under an `### Added` heading. Summarize the v1 capabilities actually shipped: one-line import-swap install (INST-01/02), fingerprint capture on passing runs (CAP-01), persistent committed baseline safe under parallel workers (CAP-02/03), weighted multi-signal scoring with an absolute confidence floor and second-best margin gate (MATCH-01/02/03), never-false-green refusal (MATCH-04), live locator rebind+replay after the real timeout (HEAL-01/02), boxed end-of-run heal/could-not-heal report (REP-01/02), fully-offline guarantee verified by a network-block test (PRIV-01), and configurable enabled/threshold/margin (CFG-01/02). Note it is a pre-1.0 release (breaking changes allowed pre-1.0). Do NOT include a real publish date claim beyond the prepared state.</action>
  <verify>
    <automated>Select-String -Path CHANGELOG.md -Pattern "0.1.0" -Quiet; if (-not $?) { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` exists with a `## [0.1.0]` section
    - The entry lists the v1 capabilities (install, capture, persistence, scoring+gates, no-false-green, live heal, report, offline, config)
    - It notes pre-1.0 semver posture (breaking changes allowed before 1.0)
  </acceptance_criteria>
  <done>CHANGELOG.md documents the 0.1.0 release with the shipped v1 feature set.</done>
</task>

<task type="auto">
  <name>Task 3: Expand README.md per D-07 (config ref, trust model, committed-baseline workflow, limitations)</name>
  <files>README.md</files>
  <read_first>
    - README.md (the Phase 1 quickstart to expand — do NOT rewrite from scratch)
    - src/config/schema.ts (VERIFIED defaults the config table must match: enabled true, threshold 0.9, margin 0.05, testIdAttr data-testid)
    - .planning/phases/03-persistence-parallel-worker-safety/03-REVIEW.md (WR-03, WR-04 — the accepted limitations to document)
    - .gitignore (the committed-baseline workflow: baseline.json committed, shards/*.tmp ignored)
    - .planning/phases/04-offline-verification-publish/04-CONTEXT.md (D-07)
  </read_first>
  <action>Expand the existing README.md (keep the current install + import-swap quickstart and reporter section — they are correct) to add, per D-07: (1) a complete config reference table covering `enabled`, `threshold`, `margin`, `testIdAttr` with their VERIFIED defaults (true / 0.9 / 0.05 / "data-testid"), plus the `SELFMEND_PRUNE` env opt-in (off by default; only prunes orphaned baselines on a complete, fully-passed run) — the current README is missing `margin` and `SELFMEND_PRUNE`, add them; (2) a "How healing works + the never-false-green trust model" section explaining capture-on-success, candidate scoring, the confidence floor AND second-best margin gate, and that below either gate the original error re-throws (no false green) and assertions are never routed through heal; (3) a "Committed baseline workflow" section explaining that `.selfmend/baseline.json` is committed (heals persist across runs and are reviewable in diffs) while `.selfmend/shards/` and `*.tmp` are gitignored transient per-worker output; (4) an honest "Limitations" section: locator-healing only, Playwright-only (v1), plus WR-03 (occurrence index counts chained-method invocations, so divergent control flow between capture and heal runs degrades to no-heal — fail-safe, never a mis-heal) and WR-04 (selectOption/setInputFiles value-payload replay edge). No docs site (D-07). No fenced-code requirement — write the config reference as a markdown table mirroring the existing one.</action>
  <verify>
    <automated>$ok = (Select-String -Path README.md -Pattern "SELFMEND_PRUNE" -Quiet) -and (Select-String -Path README.md -Pattern "margin" -Quiet) -and (Select-String -Path README.md -Pattern "Limitations" -Quiet); if (-not $ok) { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - README config table includes `enabled` (true), `threshold` (0.9), `margin` (0.05), `testIdAttr` ("data-testid") — all matching src/config/schema.ts — and documents the `SELFMEND_PRUNE` env opt-in
    - A trust-model section explains the confidence floor + second-best margin gate and the no-false-green re-throw
    - A committed-baseline section explains baseline.json is committed and shards/*.tmp are ignored
    - A Limitations section states locator-only + Playwright-only and documents WR-03 and WR-04 honestly
    - The existing quickstart + reporter sections are preserved (not rewritten away)
  </acceptance_criteria>
  <done>README.md is the launch doc per D-07: verified config reference, trust model, committed-baseline workflow, and honest limitations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer → npm publish | A manual publish must not ship stale/unbuilt or regressed dist |
| published tarball → consumer | The tarball must contain only dist (no source/maps/secrets) |
| docs → adopter trust | The README's config defaults and trust model must be accurate, or the never-false-green guarantee is undermined by wrong docs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | published dist (stale/unbuilt) | mitigate | `prepublishOnly` rebuilds + runs publint + attw before any publish (Task 1, Pattern 2) |
| T-04-06 | Information Disclosure | source maps leaking src/ paths | mitigate | `sourcemap: false` for the published build (Task 1, Pitfall 5) |
| T-04-07 | Spoofing/Repudiation | inaccurate config docs | mitigate | README config table is pinned to the VERIFIED zod schema defaults (Task 3) |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new runtime deps added (deps stay zod + picocolors, D-04); existing deps are lockfile-pinned and audited [no ASSUMED/SUS packages — research Package Legitimacy Audit] |
</threat_model>

<verification>
- `npm run build` exits 0 and `dist/` contains no `.map` files.
- `package.json` is 0.1.0 with a working `prepublishOnly` script and no new runtime deps.
- `CHANGELOG.md` has a 0.1.0 entry; `README.md` contains the config table (with margin + SELFMEND_PRUNE), trust model, committed-baseline workflow, and Limitations.
- `npx publint` and `npx attw --pack` stay green after the source-map change (full proof runs in Plan 03).
</verification>

<success_criteria>
- The package is version 0.1.0, guarded by prepublishOnly, shipping no dead source maps.
- A CHANGELOG and an expanded D-07 README make 0.1.0 a documented, trustworthy first release.
</success_criteria>

<output>
Create `.planning/phases/04-offline-verification-publish/04-02-SUMMARY.md` when done
</output>
