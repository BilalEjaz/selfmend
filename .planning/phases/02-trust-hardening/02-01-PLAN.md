---
phase: 02-trust-hardening
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/matching/decision.ts
  - src/matching/decision.test.ts
  - src/matching/types.ts
  - src/matching/scoring.test.ts
  - src/config/schema.ts
  - src/config/schema.test.ts
autonomous: true
requirements: [MATCH-02, MATCH-03, CFG-02]
must_haves:
  truths:
    - "A heal is refused with reason `below-floor` when the top candidate scores under the floor (D-03, MATCH-02)"
    - "A heal is refused with reason `ambiguous` when the top beats the runner-up by less than the margin (D-01, D-03, MATCH-03)"
    - "A solo candidate that clears the floor heals and trivially passes the margin gate (D-02)"
    - "A gap exactly equal to the margin heals (inclusive `>= margin`, mirrors the inclusive floor)"
    - "Floor is checked before margin so two below-floor candidates report `below-floor`, not `ambiguous` (D-03)"
    - "Every no-heal return carries `bestScore` (the top score seen, or null when no candidates) for the report (D-04)"
    - "An identity-preserving candidate outscores a structure-only candidate by more than the default margin (weight ordering invariant, D-09)"
    - "`margin` is a configurable global key defaulting to 0.05, validated in [0,1] beside `threshold` without renaming it (CFG-02, D-07, D-08)"
  artifacts:
    - path: "src/matching/decision.ts"
      provides: "decide(scored, { floor, margin }) with the absolute-gap margin gate and bestScore on no-heal"
      contains: "margin"
    - path: "src/matching/types.ts"
      provides: "Decision heal:false arm widened with bestScore"
      contains: "bestScore"
    - path: "src/config/schema.ts"
      provides: "margin config key + DEFAULT_MARGIN constant"
      contains: "DEFAULT_MARGIN"
  key_links:
    - from: "src/matching/decision.ts"
      to: "src/matching/types.ts"
      via: "Decision/ScoredCandidate import"
      pattern: "from \"./types"
---

<objective>
Harden the pure heal decision: add the second-best absolute-gap margin gate beside the existing confidence floor, widen the no-heal return with `bestScore`, prove the weight ordering invariant, and add the configurable `margin` key. This is the pure-core + config half of Phase 2 (MATCH-02 hardened, MATCH-03 new, CFG-02). Everything here is Playwright-free and built test-first (RED -> GREEN -> REFACTOR).

Purpose: The false-green guarantee lives in this pure core. The margin gate is the named guard against healing to the wrong element among look-alikes; it must be calibrated so an ambiguous duplicate is refused while a genuine single-survivor heal still passes.
Output: `decide(scored, { floor, margin })`, a widened `Decision` contract, a `margin` config key, and the failing-then-passing vitest suites that pin all of it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trust-hardening/02-RESEARCH.md
@.planning/phases/02-trust-hardening/02-CONTEXT.md

<interfaces>
<!-- Current pure contracts the executor extends. No codebase exploration needed. -->

From src/matching/types.ts (existing — to be widened in this plan):
- `NoHealReason = "no-fingerprint" | "no-candidates" | "below-floor" | "ambiguous"` — already declares "ambiguous"; no union change needed.
- `Decision = { heal: true; newSelector; event: HealEvent } | { heal: false; reason: NoHealReason }` — the heal:false arm gains `bestScore: number | null`.
- `ScoredCandidate = { candidate: CandidateDescriptor; score: number }`.
- `HealEvent = { newSelector; score; runnerUpScore? }` (pure, unchanged here).

From src/matching/decision.ts (existing signature, to change):
- `decide(scored: ScoredCandidate[], floor: number): Decision` -> becomes `decide(scored, { floor, margin })`.

From src/config/schema.ts (existing):
- `DEFAULT_THRESHOLD = 0.9`, `thresholdSchema` (z.number min 0 max 1), `configSchema` with `enabled`/`threshold`/`testIdAttr`.

From src/matching/scoring.ts (read-only here):
- `SIGNAL_WEIGHTS` (testId 6, text 4, role 3, attrs 2, tag 1, parentTag 0.5, neighbourSignature 0.5, ordinal 0.25); `score(fp, candidate)` -> [0,1]. Weights stay FIXED internal constants (D-09).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Add the margin gate to decide() with bestScore on every no-heal (MATCH-02, MATCH-03)</name>
  <files>src/matching/decision.ts, src/matching/decision.test.ts, src/matching/types.ts</files>
  <read_first>
    - src/matching/decision.ts (the current floor-only gate; it already sorts and retains runnerUp)
    - src/matching/types.ts (Decision union, NoHealReason, HealEvent)
    - src/matching/decision.test.ts (existing Phase 1 cases to extend, not replace)
    - .planning/phases/02-trust-hardening/02-RESEARCH.md sections "decide() Signature Change" and "Testing the Gates" (the canonical case table)
    - .planning/phases/02-trust-hardening/02-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <behavior>
    RED first: write these vitest cases against the NEW signature `decide(scored, { floor: 0.9, margin: 0.05 })` and confirm they fail before implementing.
    - Empty candidates -> `{ heal: false, reason: "no-candidates", bestScore: null }`.
    - One candidate score 0.7 -> `{ heal: false, reason: "below-floor", bestScore: 0.7 }`.
    - Two candidates 0.95 and 0.93 (gap 0.02 < 0.05) -> `{ heal: false, reason: "ambiguous", bestScore: 0.95 }`.
    - Boundary: 0.95 and 0.90 (gap exactly 0.05) -> `heal: true` (gate is `< margin` refuses, `>= margin` heals; mirrors the inclusive floor).
    - Solo above floor 0.95 (D-02) -> `heal: true`, event has no `runnerUpScore`.
    - Clear winner 0.97 and 0.80 (gap 0.17) -> `heal: true`, event `runnerUpScore: 0.80`.
    - Floor precedence: top 0.85, runner 0.84 (both below floor) -> `below-floor` (floor checked first), not `ambiguous`.
    - Preserve the existing Phase 1 floor-gate cases unchanged (update their call sites to the options-object signature).
  </behavior>
  <action>
    Widen the `Decision` `heal:false` arm in types.ts to `{ heal: false; reason: NoHealReason; bestScore: number | null }`; do NOT touch the `NoHealReason` union ("ambiguous" already exists) and do NOT touch the pure `HealEvent`. Change `decide`'s signature from `(scored, floor)` to `(scored, opts: { floor: number; margin: number })`. Logic order is load-bearing per D-03: return `no-candidates`/`bestScore:null` on empty; sort descending (copy first, keep purity); if `winner.score < opts.floor` return `below-floor` with `bestScore: winner.score`; else if a runner-up exists AND `winner.score - runnerUp.score < opts.margin` return `ambiguous` with `bestScore: winner.score` (D-01 absolute gap); else heal, attaching `runnerUpScore` only when a runner-up exists (D-02 solo case). Keep the module pure: import nothing from Playwright, node:fs, or the network. Update the existing module JSDoc to describe both gates. Do NOT add any per-test/per-call override surface (D-07 — global only).
  </action>
  <verify>
    <automated>npx vitest run src/matching/decision.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/matching/decision.test.ts` passes with all seven new cases plus the retained Phase 1 cases.
    - Ambiguous duplicate (0.95 vs 0.93) is refused with reason `ambiguous`; single survivor (0.95 vs 0.80) heals — the two load-bearing MATCH-03 behaviors.
    - `npx tsc --noEmit` clean (Decision consumers see the widened arm).
    - `decision.ts` contains no `@playwright/test`, `playwright-core`, or `node:fs` import (purity).
  </acceptance_criteria>
  <done>decide() enforces floor then margin, returns bestScore on every refusal, and the vitest suite is green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Pin the weight ordering invariant (D-09, MATCH-03 support)</name>
  <files>src/matching/scoring.test.ts</files>
  <read_first>
    - src/matching/scoring.ts (SIGNAL_WEIGHTS and score(); DO NOT change weights — D-09 keeps them fixed internal constants)
    - src/matching/scoring.test.ts (existing scorer tests to extend)
    - .planning/phases/02-trust-hardening/02-RESEARCH.md section "Multi-Signal Weighting" and the "Scorer ordering-invariant tests" paragraph
    - .planning/phases/02-trust-hardening/02-CONTEXT.md (D-09)
  </read_first>
  <behavior>
    RED first, then GREEN against the existing scorer (no scorer code change expected — these are invariant assertions over current weights):
    - An identity-preserving candidate (same testId, same role, same text, drifted tag/parent/ordinal) outscores a structure-only candidate (same tag/ordinal/parentTag, different testId, different text/role) against a shared fingerprint.
    - The score gap between those two candidates is GREATER than the default margin (0.05), proving the weights produce gaps the margin gate can act on (so a real heal among structural look-alikes clears the gate).
    - Assert by ordering and relative magnitude (`scoreA > scoreB` and `scoreA - scoreB > 0.05`), NOT by exact magic numbers, so weights can re-tune later without breaking the test.
  </behavior>
  <action>
    Add invariant tests to scoring.test.ts only; the accessibleName signal addition is OPTIONAL and DEFERRED per RESEARCH (do NOT add a Fingerprint/CandidateDescriptor field this phase — treat `text` as covering accessible name). Build two CandidateDescriptor fixtures and one Fingerprint fixture inline; the identity-preserving one shares testId/role/text, the structure-only one shares only tag/ordinal/parentTag. Assert the ordering and the >0.05 gap. Do NOT assert exact score values and do NOT modify SIGNAL_WEIGHTS or score() (D-09: weights stay fixed internal constants, not user config). Document in a test comment that this invariant is what lets the Plan 02 margin gate distinguish a true heal from a structural also-ran.
  </action>
  <verify>
    <automated>npx vitest run src/matching/scoring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/matching/scoring.test.ts` passes including the new ordering-invariant cases.
    - The identity-preserving candidate's score exceeds the structure-only candidate's by more than 0.05.
    - `SIGNAL_WEIGHTS` and `score()` in scoring.ts are unchanged (git diff shows scoring.ts source untouched; only scoring.test.ts changed).
  </acceptance_criteria>
  <done>The weight ordering invariant is asserted relatively (identity dominates structure by >0.05) and green, with the scorer source untouched.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Add the margin config key (CFG-02, D-07/D-08)</name>
  <files>src/config/schema.ts, src/config/schema.test.ts</files>
  <read_first>
    - src/config/schema.ts (DEFAULT_THRESHOLD, thresholdSchema, configSchema — copy the threshold pattern)
    - src/config/schema.test.ts (existing config validation tests to extend)
    - .planning/phases/02-trust-hardening/02-RESEARCH.md sections "Config Schema Extension" and "Default Margin Calibration" (default 0.05)
    - .planning/phases/02-trust-hardening/02-CONTEXT.md (D-07, D-08)
  </read_first>
  <behavior>
    RED first, then GREEN:
    - `configSchema.parse({})` yields `margin === 0.05` (default applied).
    - `margin: -0.1` and `margin: 1.5` are rejected with readable zod messages ("margin must be >= 0" / "margin must be <= 1").
    - `margin: "high"` (wrong type) is rejected with a readable "margin must be a number" message.
    - `margin: 0.05` and the boundaries `0` and `1` are accepted.
    - The existing `threshold` key still defaults to 0.9 and is NOT renamed (back-compat assertion).
  </behavior>
  <action>
    Add a `DEFAULT_MARGIN = 0.05` exported constant beside `DEFAULT_THRESHOLD`, documented with the same "raising = safer, lowering trades safety for green-ness" warning the threshold carries and noting it is the absolute second-best gap in [0,1] score units (D-01). Add a `margin` key to `configSchema` by reusing the `thresholdSchema` zod pattern (a z.number min 0 max 1 with margin-specific messages: "margin must be a number" / "must be >= 0" / "must be <= 1"), defaulting to `DEFAULT_MARGIN`. Keep `threshold` exactly as-is (D-08: do not rename the floor key). Keep `margin` global only — no per-test override surface (D-07). `SelfmendConfig` is inferred, so the type updates automatically.
  </action>
  <verify>
    <automated>npx vitest run src/config/schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/config/schema.test.ts` passes including the new margin default, range, and type cases.
    - `configSchema.parse({}).margin === 0.05` and `configSchema.parse({}).threshold === 0.9` (threshold unchanged).
    - Out-of-range and wrong-type margins are rejected with readable messages (ASVS V5 input validation across the config trust boundary).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>`margin` is a validated global config key defaulting to 0.05 beside the untouched `threshold`, with green config tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user config -> plugin | The `margin` number (like `threshold`) is user-supplied and crosses into the heal-decision core. |
| scored candidates -> decide() | The pure decision is the in-code seat of the false-green guarantee. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `margin` config value | mitigate | Validate `margin` in [0,1] via the zod `thresholdSchema` pattern; reject NaN/out-of-range/wrong-type with readable messages before it reaches `decide()` (ASVS V5). |
| T-02-02 | Tampering | `decide()` gate logic | mitigate | Floor-then-margin order pinned by tests (D-03); a below-floor or ambiguous pair returns `heal:false` so a mis-implemented gate cannot silently produce a false green. Boundary `>= margin` case tested. |
| T-02-03 | Information Disclosure | `bestScore` on no-heal | accept | `bestScore` is a derived number in [0,1], not raw DOM content; safe to surface (consistent with the Phase 1 derived-signals-only contract). |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No net-new packages this phase (RESEARCH Package Legitimacy Audit: N/A); no install task, so no slopcheck checkpoint required. |
</threat_model>

<verification>
- `npx vitest run src/matching/decision.test.ts src/matching/scoring.test.ts src/config/schema.test.ts` all green.
- `npx tsc --noEmit` clean across the widened `Decision` contract.
- `decision.ts` and `scoring.ts` contain no Playwright/node:fs/network imports (purity preserved).
- Default margin 0.05 calibration sanity: the identity-vs-structure gap exceeds 0.05 (Task 2) and the ambiguous 0.95/0.93 pair is refused (Task 1). Cross-fixture confirmation that the existing single-survivor heal still heals is proven in Plan 02's integration test.
</verification>

<success_criteria>
- decide() enforces both gates (floor then margin, D-03), returns `bestScore` on every refusal, and emits `ambiguous` for within-margin look-alikes (MATCH-02, MATCH-03).
- The scorer weight ordering invariant is asserted relatively (identity > structure by >0.05), keeping weights fixed internal constants (D-09).
- `margin` is a validated global config key defaulting to 0.05 beside an unchanged `threshold` (CFG-02, D-07, D-08).
- All three vitest suites are green and the pure core stays Playwright-free.
</success_criteria>

<output>
Create `.planning/phases/02-trust-hardening/02-01-SUMMARY.md` when done.
</output>
