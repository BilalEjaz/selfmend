---
phase: 01-thinnest-real-heal
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - spike/rebind-spike.spec.ts
  - spike/FINDINGS.md
autonomous: true
requirements: [HEAL-01, HEAL-02]
user_setup: []

must_haves:
  truths:
    - "A wrapped click on a deleted-then-renamed selector throws a real TimeoutError that can be caught"
    - "After catching that TimeoutError, page.locator(newSelector).click() replays green"
    - "A Locator Proxy re-wraps chained locators (getByRole(...).first()) without losing healing or breaking strict-mode/auto-wait"
    - "The timeout-budget split between the real attempt and the heal replay is measured and bounded"
    - "expect(locator) assertion matchers do NOT route through the wrapped action heal path"
  artifacts:
    - path: "spike/rebind-spike.spec.ts"
      provides: "Throwaway proof of catch-and-replay against @playwright/test 1.60"
      min_lines: 40
    - path: "spike/FINDINGS.md"
      provides: "Locked timing/mechanics decisions feeding plan 04"
      min_lines: 20
  key_links:
    - from: "spike/rebind-spike.spec.ts"
      to: "tests/fixture-app/broken.html"
      via: "mutated-selector fixture"
      pattern: "broken\\.html"
---

<objective>
Run the research-mandated ~1-day THROWAWAY SPIKE to lock the three live-rebind timing/mechanics details against `@playwright/test@1.60` BEFORE the integration code is written. This is throwaway code in `spike/`, deleted after its findings are captured in `spike/FINDINGS.md`, which feeds plan 04. HEAL-01 / HEAL-02 mechanics de-risk.

Purpose: De-risk the project's riskiest unknown (the live locator-rebind hook) on the real engine, so plan 04's integration tasks are written against confirmed behavior, not assumptions.
Output: A throwaway spike spec proving catch-and-replay + chained-proxy + bounded timeout budget + assertion-path exclusion, and a FINDINGS.md.

NOTE: This plan produces THROWAWAY code. Do not build production modules here. Findings transfer to plan 04; the `spike/` directory is deleted at the end of the phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-thinnest-real-heal/01-RESEARCH.md
@.planning/phases/01-thinnest-real-heal/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spike — catch TimeoutError + replay, and chained-Proxy re-wrap</name>
  <files>spike/rebind-spike.spec.ts</files>
  <read_first>
    - .planning/phases/01-thinnest-real-heal/01-RESEARCH.md (## THE LIVE-REBIND HOOK Q1–Q3, ### Spike recommendation, Pattern 1, Pattern 2)
    - tests/fixture-app/broken.html, tests/fixture-app/index.html
  </read_first>
  <action>
    Against `@playwright/test@1.60`, write a throwaway spec that: (1) wraps a real `page.locator(...).click()` so the real action runs to its configured timeout, catches `errors.TimeoutError` (verify via `instanceof errors.TimeoutError` AND `error.name === 'TimeoutError'`) on a selector that resolves on `index.html` but is renamed in `broken.html`, then constructs a fresh `page.locator(newSelector)` from the surviving semantic element and replays the click to green. Do NOT pre-check `count()` — let auto-wait run to timeout (the HEAL-02 mechanism). (2) Build a minimal `Proxy`-over-Locator per research Pattern 2 and confirm chaining (`page.getByRole('button',{name:'Submit'}).first()`) returns a re-wrapped, still-healing locator and does not break strict-mode/auto-wait. (3) Confirm `expect(locator).toBeVisible()` resolves through matchers and does NOT pass through the wrapped action set (assertions stay sacred — open question Q2 in research). Read the three GitHub prior-art repos' source if reachable; otherwise note unreachable.
  </action>
  <verify>
    <automated>npx playwright test spike/rebind-spike.spec.ts</automated>
  </verify>
  <done>Spike spec passes: TimeoutError caught and replay goes green; chained proxy re-wraps; assertion path excluded.</done>
</task>

<task type="auto">
  <name>Task 2: Measure timeout budget and record FINDINGS</name>
  <files>spike/FINDINGS.md, spike/rebind-spike.spec.ts</files>
  <read_first>
    - .planning/phases/01-thinnest-real-heal/01-RESEARCH.md (Q2 timeout-budget caveat; ## Open Questions 1 and 2)
  </read_first>
  <action>
    Extend the spike to measure the timeout-budget split: time the real attempt (runs to the configured timeout) vs the heal enumeration + replay, and pin the decision research flagged as open — whether to pass an explicit `{ timeout }` on the first attempt or rely on config/testInfo defaults, and what replay timeout keeps total wall-clock bounded. Record in `spike/FINDINGS.md`: (a) the exact `TimeoutError` detection idiom to use in plan 04, (b) the chosen timeout-budget strategy with measured numbers, (c) the confirmed action-method set vs chaining-method set vs assertion exclusion, (d) any prior-art gotchas found, (e) the precise rebind selector-construction approach (prefer test-id, then stable attr, then scoped nth; uniqueness checked via count()===1 but acceptance gated on score). These findings are the contract plan 04 implements.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('spike/FINDINGS.md','utf8');if(!/timeout/i.test(t)||!/TimeoutError/.test(t))process.exit(1);console.log('ok')"</automated>
  </verify>
  <done>FINDINGS.md records the detection idiom, bounded timeout-budget strategy with measured numbers, method-set partition, and rebind selector approach for plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wrapped action → real Playwright engine | Throwaway probe of the real interception seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | premature heal on poll miss | mitigate | Spike proves heal fires only after the real TimeoutError, never on a transient miss (HEAL-02 mechanism locked) |
| T-03-02 | Integrity | assertion path accidentally healed | mitigate | Spike confirms expect(locator) matchers bypass the wrapped action set; finding feeds plan 04's method partition |
| T-03-03 | Denial of service | unbounded heal wall-clock | mitigate | Spike measures and bounds the real-attempt vs replay timeout split |
</threat_model>

<verification>
- `npx playwright test spike/rebind-spike.spec.ts` passes.
- `spike/FINDINGS.md` records detection idiom, bounded budget, method partition, rebind approach.
</verification>

<success_criteria>
- Catch-and-replay proven on PW 1.60 against a real mutated-selector fixture.
- Chained-Proxy re-wrap and assertion-path exclusion confirmed.
- Timeout budget measured and bounded; findings recorded for plan 04.
</success_criteria>

<output>
Create `.planning/phases/01-thinnest-real-heal/01-03-SUMMARY.md` when done. Note that `spike/` is throwaway and slated for deletion once plan 04 consumes FINDINGS.md.
</output>
