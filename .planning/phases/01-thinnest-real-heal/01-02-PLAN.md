---
phase: 01-thinnest-real-heal
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/matching/types.ts
  - src/matching/scoring.ts
  - src/matching/scoring.test.ts
  - src/matching/decision.ts
  - src/matching/decision.test.ts
autonomous: true
requirements: [MATCH-01]
user_setup: []

must_haves:
  truths:
    - "A candidate that is a strong match for a fingerprint scores high (near 1)"
    - "A candidate sharing little with the fingerprint scores low"
    - "When no candidate clears the conservative floor the decision is no-heal with a reason (D-09, false-green guard)"
    - "When the original element is genuinely gone (no good candidate) the decision is no-heal"
    - "Two near-identical ambiguous candidates do not produce a confident single winner"
    - "The scorer and decision modules import nothing from Playwright or fs"
  artifacts:
    - path: "src/matching/types.ts"
      provides: "Fingerprint + CandidateDescriptor + ScoredCandidate + Decision types"
      exports: ["Fingerprint", "CandidateDescriptor", "ScoredCandidate", "Decision"]
    - path: "src/matching/scoring.ts"
      provides: "Pure weighted scorer fingerprint x candidate -> 0..1"
      exports: ["score"]
    - path: "src/matching/decision.ts"
      provides: "Pure heal/no-heal decision over scored candidates given a floor"
      exports: ["decide"]
    - path: "src/matching/scoring.test.ts"
      provides: "TDD suite for scorer"
      min_lines: 30
    - path: "src/matching/decision.test.ts"
      provides: "TDD suite for decision (below-floor, removed-element, ambiguous)"
      min_lines: 30
  key_links:
    - from: "src/matching/decision.ts"
      to: "src/matching/scoring.ts"
      via: "decide consumes score outputs"
      pattern: "ScoredCandidate"
---

<objective>
Build the pure, Playwright-free heart of the product: the weighted scorer (`score(fingerprint, candidate) -> 0..1`) and the heal-decision module (`decide(scored[], floor) -> heal | no-heal + reason`), both test-first (TDD). This is the core IP and where the defining false-green guarantee is enforced (conservative floor now; second-best margin gate is Phase 2). MATCH-01.

Purpose: A deterministic, offline, unit-testable decision core that can rank candidates and refuse to heal when confidence is insufficient — independent of any browser.
Output: `types.ts` contracts, `scoring.ts`, `decision.ts`, and their Vitest suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-thinnest-real-heal/01-CONTEXT.md
@.planning/phases/01-thinnest-real-heal/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define pure matching contracts (interface-first)</name>
  <files>src/matching/types.ts</files>
  <read_first>
    - .planning/phases/01-thinnest-real-heal/01-RESEARCH.md (### Recommended Project Structure; Q3 candidate descriptors; Code Examples capture fingerprint shape)
    - .planning/phases/01-thinnest-real-heal/01-CONTEXT.md (CAP-01 signal list: text, role, test-id, attributes, neighbour context, DOM position)
  </read_first>
  <action>
    Define the shared, pure types that scoring, decision, capture (plan 04), and candidate-finder (plan 04) all build against — no implementation, no Playwright imports. `Fingerprint`: tag, role, normalized text, testId, key attrs map, ordinal/DOM-position, parentTag, neighbour signature. `CandidateDescriptor`: same observable signals PLUS a `uniqueSelector` string (the uniquely-resolving selector that rebind will pass to `page.locator()`). `ScoredCandidate`: `{ candidate: CandidateDescriptor; score: number }`. `Decision`: discriminated union `{ heal: true; newSelector; event } | { heal: false; reason }` where `reason` enumerates `no-fingerprint`, `no-candidates`, `below-floor`, `ambiguous` (ambiguous reserved for Phase 2 margin gate). Export all. These are the contracts downstream plans consume directly — no codebase exploration needed.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>types.ts exports Fingerprint, CandidateDescriptor, ScoredCandidate, Decision; compiles with no Playwright/fs import.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Pure weighted scorer (RED→GREEN→REFACTOR)</name>
  <files>src/matching/scoring.ts, src/matching/scoring.test.ts</files>
  <read_first>
    - .planning/phases/01-thinnest-real-heal/01-RESEARCH.md (## Don't Hand-Roll key insight on the pure scorer; Similo/Healenium scoring references)
    - src/matching/types.ts (the contracts from Task 1)
  </read_first>
  <behavior>
    - Test: identical signals (same testId, role, text, attrs) score at/near 1.
    - Test: a candidate matching strong-weight signals (testId, role+name) but differing on volatile ones still scores high.
    - Test: a candidate sharing only a weak signal scores low.
    - Test: a candidate with no overlapping signals scores near 0.
    - Test: scoring is deterministic — same inputs yield the same number; output is bounded 0..1.
    - Test: text similarity uses normalized comparison (token overlap / edit distance), not raw equality, so minor text drift still scores partial.
  </behavior>
  <action>
    RED: write `scoring.test.ts` for the behaviors above against the not-yet-written `score`; confirm failure.
    GREEN: implement `score(fingerprint, candidate): number` as a deterministic weighted sum of per-signal sub-scores, normalized to 0..1. Weight high-stability identity signals (testId, computed role + accessible name) above volatile ones (class tokens, ordinal). Hand-roll text/attribute similarity (token overlap / Levenshtein) per research — no dependency. Keep the module pure: no Playwright, no fs, no network.
    REFACTOR: extract per-signal scorers and the weight table to named constants so Phase 2 can re-weight without rewrites.
    Commit RED then GREEN separately.
  </action>
  <verify>
    <automated>npx vitest run src/matching/scoring.test.ts</automated>
  </verify>
  <done>Scorer green; bounded 0..1, deterministic, identity-signal-weighted; no Playwright/fs import.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Pure heal-decision with conservative floor (RED→GREEN→REFACTOR)</name>
  <files>src/matching/decision.ts, src/matching/decision.test.ts</files>
  <read_first>
    - .planning/phases/01-thinnest-real-heal/01-RESEARCH.md (### Pitfall 2 false green; Architectural Responsibility Map scoring+decision tier)
    - .planning/phases/01-thinnest-real-heal/01-CONTEXT.md (D-09 lean-safe posture)
    - src/matching/scoring.ts, src/matching/types.ts
  </read_first>
  <behavior>
    - Test (removed element): no scored candidate near the fingerprint -> `{ heal: false, reason: 'no-candidates' }` or `'below-floor'` (the genuinely-gone case must NOT heal — false-green guard).
    - Test (below floor): best candidate scores under the floor -> `{ heal: false, reason: 'below-floor' }`.
    - Test (clear winner): one candidate well above the floor -> `{ heal: true, newSelector: <its uniqueSelector> }`.
    - Test (ambiguous duplicates): two candidates both above floor and within a hair of each other -> Phase-1 behavior must still pick the top by score (margin gate is Phase 2) but the design must surface the runner-up so Phase 2 can add the margin reason without rewriting.
    - Test (empty scored list): -> `{ heal: false, reason: 'no-candidates' }`.
  </behavior>
  <action>
    RED: write `decision.test.ts` for the behaviors above against the not-yet-written `decide`; confirm failure.
    GREEN: implement `decide(scored: ScoredCandidate[], floor: number): Decision`. Sort by score desc; if empty -> no-candidates; if top < floor -> below-floor; else heal with the winner's `uniqueSelector` as `newSelector` and build the heal event payload. Keep the runner-up score in scope so Phase 2 can layer the margin gate. PURE — no Playwright/fs/network. This module is where Pitfall 2 (false green) is prevented in code per D-09.
    REFACTOR: name the floor/decision constants; keep the discriminated union exhaustive.
    Commit RED then GREEN separately.
  </action>
  <verify>
    <automated>npx vitest run src/matching/decision.test.ts</automated>
  </verify>
  <done>Decision green; removed-element and below-floor both refuse to heal; clear winner heals to its uniqueSelector; runner-up retained for Phase 2; pure module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate signals → decision | DOM-derived (untrusted) signals feed the heal/no-heal decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Repudiation / integrity | decision.ts (false green) | mitigate | Conservative floor gated in code; genuinely-gone and below-floor refuse to heal (TDD-proven) |
| T-02-02 | Tampering | scoring.ts determinism | mitigate | Pure, deterministic, dependency-free scorer; bounded 0..1 verified by tests |
| T-02-03 | Information disclosure | types.ts Fingerprint | accept | Types only here; PII-minimization enforced at capture (plan 04). No raw DOM stored by these modules |
</threat_model>

<verification>
- `npx vitest run src/matching` is fully green.
- `npx tsc --noEmit` confirms scoring/decision/types import nothing from Playwright or fs.
</verification>

<success_criteria>
- Pure scorer: deterministic, 0..1, identity-signal-weighted, test-first.
- Pure decision: conservative floor refuses to heal a genuinely-gone or below-floor element (false-green guard, test-first).
- Contracts (types.ts) ready for capture, candidate-finder, fixture in plan 04.
</success_criteria>

<output>
Create `.planning/phases/01-thinnest-real-heal/01-02-SUMMARY.md` when done.
</output>
