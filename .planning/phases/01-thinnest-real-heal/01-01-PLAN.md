---
phase: 01-thinnest-real-heal
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsdown.config.ts
  - vitest.config.ts
  - playwright.config.ts
  - .gitignore
  - src/config/schema.ts
  - src/config/schema.test.ts
  - src/config/defaults.ts
  - tests/fixture-app/index.html
  - tests/fixture-app/broken.html
autonomous: false
requirements: [INST-01, CFG-01]
user_setup: []

must_haves:
  truths:
    - "Repo builds dual ESM+CJS with type declarations via a single build command"
    - "Config defaults to enabled:true and a conservative threshold (~0.9)"
    - "Setting enabled:false in config parses to a disabled state"
    - "Invalid config (threshold out of 0..1, wrong types) is rejected with a clear zod error"
    - "A local static HTML fixture exists with a known element and a selector-mutated variant"
  artifacts:
    - path: "package.json"
      provides: "Dual exports map, peerDependency on @playwright/test, runtime deps zod+picocolors, no postinstall"
      contains: "peerDependencies"
    - path: "src/config/schema.ts"
      provides: "zod schema for plugin config"
      exports: ["SelfmendConfig", "configSchema"]
    - path: "src/config/defaults.ts"
      provides: "Default config values"
      exports: ["defaultConfig"]
    - path: "src/config/schema.test.ts"
      provides: "TDD suite for config parse/validate"
      min_lines: 20
    - path: "tests/fixture-app/index.html"
      provides: "Static page with a stable target element for capture"
  key_links:
    - from: "src/config/defaults.ts"
      to: "src/config/schema.ts"
      via: "defaults satisfy configSchema"
      pattern: "configSchema\\.parse"
---

<objective>
Stand up the `selfmend` package skeleton (dual ESM/CJS via tsdown, peerDependency on `@playwright/test`, runtime deps `zod`+`picocolors`, no network/postinstall) and the test-first config layer (CFG-01: `enabled` toggle, conservative `threshold` default ~0.9). Also create the Wave 0 framework configs and the local static HTML fixture app that every integration test in this phase runs against.

Purpose: A buildable, importable foundation plus a validated single source of truth for config. This is the first half of the Walking Skeleton's "package skeleton resolves" proof.
Output: Build + test toolchain, `package.json` with correct `exports`, validated config module, and the offline HTML fixture app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-thinnest-real-heal/01-CONTEXT.md
@.planning/phases/01-thinnest-real-heal/01-RESEARCH.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Package legitimacy gate before installs</name>
  <read_first>
    - .planning/phases/01-thinnest-real-heal/01-RESEARCH.md (## Package Legitimacy Audit, ## Standard Stack)
  </read_first>
  <what-built>Nothing yet — this is the supply-chain gate that must clear before any `pnpm add`/`npm install` runs.</what-built>
  <how-to-verify>
    RESEARCH ran the Package Legitimacy Audit manually (slopcheck offline-unavailable). All packages are mainstream with authoritative repos but were not slopcheck-verified, so confirm them before install:
    1. Confirm `tsdown` at npmjs.com/package/tsdown (repo github.com/rolldown/tsdown).
    2. Confirm `zod` at npmjs.com/package/zod, `picocolors` at npmjs.com/package/picocolors.
    3. Confirm `vitest`, `publint`, `@arethetypeswrong/cli` exist and are the expected projects.
    4. typescript and @playwright/test are Microsoft-published — spot check only.
  </how-to-verify>
  <acceptance_criteria>
    - Reviewer confirms every package in the audit table resolves to its stated source repo.
    - Any package that cannot be confirmed is treated as [ASSUMED] and blocks until verified.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with installs, or name any package to investigate.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Scaffold dual ESM/CJS package + framework configs + fixture app</name>
  <files>package.json, tsconfig.json, tsdown.config.ts, vitest.config.ts, playwright.config.ts, .gitignore, tests/fixture-app/index.html, tests/fixture-app/broken.html</files>
  <read_first>
    - .planning/phases/01-thinnest-real-heal/01-RESEARCH.md (## Standard Stack, ## Validation Architecture, ### Recommended Project Structure)
    - .planning/research/STACK.md (## Installation, ## Publishing/Versioning/Configuration)
  </read_first>
  <action>
    Initialize the `selfmend` package (name per D-01, public import path per D-02). Pin majors resolving the research version drift (assumption A3): use `typescript@^6` (npm latest 6.0.3), `vitest@^4` (latest 4.1.7), `tsdown@^0.22`, `zod@^4`, `picocolors@^1`. Declare `@playwright/test` as `peerDependencies` at `>=1.42` and as a `devDependency` at `^1.60` for local testing — never as a regular dependency. Set `engines.node` to `>=22`, `type` to `module`, `license` to MIT.
    Configure tsdown to emit ESM + CJS + `.d.ts` into `dist/`; write the `exports` map with the `types` condition first, then `import`, then `require`. Ensure NO `postinstall` script and no network access in any script (the offline guarantee is a security property).
    tsconfig: `strict: true`, `module: nodenext`, `moduleResolution: nodenext`, `declaration: true`.
    vitest.config.ts: scope test discovery to `src/**/*.test.ts` (pure-logic only; keep Playwright tests out of vitest).
    playwright.config.ts: single worker for Phase 1 (`workers: 1`), chromium project, `testDir: tests`, register the selfmend reporter slot as a comment placeholder (wired in plan 05). Serve `tests/fixture-app/` via Playwright's static file mechanism or `file://` — keep fully offline, no dev server hitting the network.
    Fixture app: `tests/fixture-app/index.html` holds a button with a stable, fingerprintable identity (text "Submit", a `data-testid`, a role). `tests/fixture-app/broken.html` is the same page with that button's primary selector mutated (e.g. test-id renamed, class changed) but the same semantic element present — this is the regression net for healing. Add a control element that is genuinely absent in broken.html for the no-false-green test in later plans.
    Run the install per STACK.md (pnpm preferred, npm fallback) and `playwright install chromium`.
  </action>
  <verify>
    <automated>npx tsdown --help &gt;NUL 2&gt;&amp;1 &amp;&amp; node -e "const p=require('./package.json');if(!p.peerDependencies||!p.peerDependencies['@playwright/test'])process.exit(1);if(p.dependencies&amp;&amp;p.dependencies['@playwright/test'])process.exit(1);if(p.scripts&amp;&amp;p.scripts.postinstall)process.exit(1);if(!p.exports)process.exit(1);console.log('ok')"</automated>
  </verify>
  <done>Build toolchain installed; package.json has dual exports, peerDependency (not dependency) on @playwright/test, no postinstall; vitest/playwright configs present; fixture-app index.html + broken.html exist.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Config schema + defaults (RED→GREEN→REFACTOR)</name>
  <files>src/config/schema.ts, src/config/schema.test.ts, src/config/defaults.ts</files>
  <read_first>
    - .planning/phases/01-thinnest-real-heal/01-CONTEXT.md (D-08 on-by-default, D-09 conservative ~0.9 threshold)
    - .planning/phases/01-thinnest-real-heal/01-RESEARCH.md (CFG-01 row in phase_requirements; Security Domain V5 input validation)
  </read_first>
  <behavior>
    - Test: parsing an empty object yields defaults `enabled: true`, `threshold` at the conservative default (~0.9), and a default `testIdAttr` of `data-testid`.
    - Test: `{ enabled: false }` parses to a disabled config (heal toggle off per CFG-01).
    - Test: `threshold` outside 0..1 is rejected with a zod error.
    - Test: wrong types (e.g. `enabled: "yes"`, `threshold: "high"`) are rejected.
    - Test: `defaultConfig` itself passes `configSchema.parse` without modification.
  </behavior>
  <action>
    RED: write `src/config/schema.test.ts` covering the behaviors above against not-yet-existing exports; confirm it fails.
    GREEN: define `configSchema` in `src/config/schema.ts` using zod — fields `enabled` (boolean, default true per D-08), `threshold` (number, range 0..1, default the conservative value per D-09), `testIdAttr` (string, default `data-testid`). Export the inferred `SelfmendConfig` type. Define `defaultConfig` in `src/config/defaults.ts` as the parsed defaults. Keep this module Playwright-free and fs-free so it is unit-testable in isolation. Implement per D-XX decisions; the exact threshold number is a documented default (assumption A2), calibration is Phase 2.
    REFACTOR: extract any shared range/string constraints; keep zod messages readable.
    Commit RED then GREEN as separate commits.
  </action>
  <verify>
    <automated>npx vitest run src/config</automated>
  </verify>
  <done>All config tests green; defaults are enabled:true + conservative threshold; invalid config rejected; defaultConfig round-trips through the schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local install | Third-party packages enter the build (supply chain) |
| user config object → plugin | Untrusted config values cross into the heal decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | pnpm/npm installs | mitigate | Task 0 blocking-human legitimacy gate; no postinstall scripts; offline-only scripts |
| T-01-01 | Tampering | config parsing (src/config/schema.ts) | mitigate | zod-validate all config; reject out-of-range threshold and wrong types (ASVS V5) |
| T-01-02 | Information disclosure | package scripts / build | mitigate | No network in any npm script; offline guarantee is a security property |
| T-01-03 | Elevation | peerDependency vs dependency on @playwright/test | mitigate | Declare as peerDependency only; verified by build check so the plugin never bundles its own Playwright |
</threat_model>

<verification>
- `npx vitest run src/config` is green.
- package.json build check passes (peer not dependency, no postinstall, exports present).
- `tests/fixture-app/index.html` and `broken.html` exist and are openable offline.
</verification>

<success_criteria>
- Package builds dual ESM/CJS with declarations.
- Config defaults enabled:true + conservative threshold; CFG-01 toggle works; invalid config rejected (test-first).
- Offline HTML fixture app present for downstream integration tests.
</success_criteria>

<output>
Create `.planning/phases/01-thinnest-real-heal/01-01-SUMMARY.md` when done.
</output>
